# v14 版本开发计划 - 当前会话功能问题排查与修复

## 版本管理

| 版本 | 状态 | 发布日期 | 说明 |
|------|------|----------|------|
| v14.0.0 | 待执行 | - | 当前会话功能问题排查与全面修复 |
| v13.0.0 | 已完成 | 2026-03-06 | 历史功能稳定版 |

---

## v14.0.0 - 当前会话功能问题排查与修复

排查并修复当前会话界面聊天功能的问题，确保功能稳定可靠。

**需求文档**: docs/需求文档/当前会话功能/当前会话-主需求.md
**文档命名规范**: docs/文档命名规范.md
**设计编写规范**: docs/技术设计/设计规范.md
**开发规范**: docs/开发/开发规范.md

---

## 角色说明

| 角色 | 职责 | 适用任务 |
|------|------|----------|
| 产品经理 | 需求分析、产品设计 | 需求检查与修复 |
| 架构师 | 系统架构设计、技术方案评审 | 设计检查与修复 |
| 全栈工程师 | 代码质量评审、功能实现 | 代码检查与修复 |
| 技术经理 | 技术任务规划、代码实现与修复 | 测试与修复 |
| 产品总监 | 战略规划、任务分配、产出评审 | 汇总评审、最终验收 |

---

## 功能任务组总览

| 任务组 | 功能模块 | 优先级 | 状态 |
|--------|---------|--------|------|
| T1 | 任务执行与流式输出 | P0 | 待执行 |
| T2 | 会话历史管理 | P1 | 待执行 |
| T3 | 会话状态与恢复 | P1 | 待执行 |
| T4 | 消息渲染与交互 | P1 | 待执行 |
| T5 | 安全与权限控制 | P0 | 待执行 |
| T6 | 集成验证与测试 | P1 | 待执行 |

---

## T1 - 任务执行与流式输出

**功能模块**: 基本任务执行、POST /api/task 接口、POST /api/task/stream SSE 流式输出

### T1.1 任务执行流程需求检查与修复

**执行角色**: 产品经理

- [x] 检查并修复任务执行流程需求文档完整性
- [x] 检查并修复流式输出的触发条件和场景定义
- [x] 检查并修复错误处理和超时场景的需求说明

**检查结果**: ✅ 需求文档完整，无需修复
- 任务执行流程需求文档完整性：覆盖从入口到完成的完整流程
- 流式输出的触发条件和场景定义：11种场景定义完整，包含状态转换
- 错误处理和超时场景的需求说明：4个错误级别，3种超时类型定义完整

### T1.2 任务参数与响应需求检查与修复

**执行角色**: 产品经理

- [x] 检查并修复任务请求参数（prompt、working_dir、tools）的需求定义
- [x] 检查并修复任务响应格式（success、message、files_changed、tools_used）的需求定义

**检查结果**: ✅ 需求文档定义完整，无需修复
- 任务请求参数：prompt、working_dir、tools 的类型、必填性、约束条件均已完整定义
- 任务响应格式：success、message、files_changed、tools_used 等字段均已完整定义
- 验证规则：长度/格式限制和验证规则均已明确定义

### T1.3 客户端封装设计检查与修复

**执行角色**: 架构师

- [x] 检查并修复 ClaudeCodeClient 封装设计是否符合规范
- [x] 检查并修复 SSE 流式输出数据格式设计
- [x] 检查并修复异步迭代器模式的使用

**检查结果**: ✅ 符合设计规范，无需修复
- ClaudeCodeClient 封装设计：职责清晰，初始化参数合理，配置管理规范
- SSE 流式输出数据格式：7 种消息类型完整，StreamMessage 结构规范，与需求文档一致
- 异步迭代器模式：正确使用 AsyncIterator，异常处理完整，会话完整性延迟设计合理

### T1.4 消息类型与环境变量设计检查与修复

**执行角色**: 架构师

- [x] 检查并修复消息类型（TEXT/TOOL_USE/ERROR/COMPLETE）映射设计
- [x] 检查并修复环境变量管理（CLAUDECODE）的设计方案

**检查结果**: ✅ 符合设计规范，无需修复

- 消息类型映射设计：
  - 代码实现定义了 7 种消息类型（TEXT/TOOL_USE/TOOL_RESULT/THINKING/ERROR/COMPLETE/ASK_USER_QUESTION）
  - 与需求文档 `当前会话-消息类型.md` 定义完全一致
  - `info` 类型按需求文档标注为"规划中"，代码未实现，设计一致

- 环境变量管理（CLAUDECODE）设计方案：
  - 实现了完整的三阶段隔离策略（调用前清除 → 执行中覆盖 → 调用后恢复）
  - 代码实现位置：`client.py` 第 304-307 行（清除）、159-175 行（覆盖配置）、461-464 行（恢复）
  - 异常处理完善，在 `finally` 块中确保环境变量恢复
  - 日志记录详细，便于调试和问题追踪

### T1.5 run() 与 run_stream() 代码检查与修复

**执行角色**: 全栈工程师

- [x] 检查并修复 run() 方法执行逻辑
- [x] 检查并修复 run_stream() 方法流式输出逻辑
- [x] 检查并修复消息类型映射完整性

**检查结果**: ✅ 代码实现正确，无需修复
- run() 方法：正确调用 run_stream()，正确收集 TEXT 消息，正确提取 COMPLETE metadata，正确处理 ERROR
- run_stream() 方法：CLAUDECODE 环境变量隔离正确，正确处理所有内容块类型，tool_use_id 配对机制正确，会话完整性延迟(20秒)正确实现
- 消息类型映射：7种消息类型全部实现，与需求文档完全一致

### T1.6 环境变量与跟踪逻辑代码检查与修复

**执行角色**: 全栈工程师

- [x] 检查并修复环境变量保存和恢复逻辑
- [x] 检查并修复工具使用跟踪（_track_tool_use）逻辑
- [x] 检查并修复文件变更跟踪逻辑

**检查结果**: ✅ 代码实现正确，无需修复
- 环境变量保存/恢复：使用 `os.environ.pop()` 保存，`finally` 块恢复，异常安全
- 工具使用跟踪：去重逻辑正确，覆盖所有工具调用
- 文件变更跟踪：覆盖 Write/Edit/Bash 三种工具，命令解析完整

### T1.7 ClaudeCodeClient 测试与修复

**执行角色**: 技术经理

- [x] 检查并修复 ClaudeCodeClient 基本执行逻辑测试
- [x] 检查并修复流式消息生成和格式化测试
- [x] 检查并修复消息类型枚举和映射测试

**检查结果**: 测试文件完整，无需修复

测试文件: `tests/test_client.py` (共 1246 行，15 个测试类，100+ 测试用例)

1. **消息类型枚举和映射测试** (TestMessageType, TestQuestionStatus):
   - 7 种消息类型 (TEXT/TOOL_USE/TOOL_RESULT/THINKING/ERROR/COMPLETE/ASK_USER_QUESTION) 与需求文档一致
   - QuestionStatus 枚举 (PENDING/SHOWING/SHOWING_UPPER/ANSWERED/TIMEOUT/CANCELLED) 完整
   - 枚举值验证、字符串比较、类型检查等测试完整

2. **流式消息生成和格式化测试** (TestStreamMessage, TestTaskResult, TestQuestionOption 等):
   - StreamMessage 数据类所有字段测试完整 (type/content/timestamp/tool_name/tool_input/tool_use_id/metadata/question)
   - TaskResult 数据类所有字段测试完整 (success/message/session_id/model/cost_usd/duration_ms/files_changed/tools_used)
   - 所有 7 种消息类型的 StreamMessage 创建和字段测试完整
   - timestamp 自动生成测试完整
   - tool_use_id 配对机制测试完整

3. **ClaudeCodeClient 基本执行逻辑测试** (TestClaudeCodeClientInit, TestClaudeCodeClientSessionManagement, TestClaudeCodeClientToolTracking 等):
   - 初始化参数测试 (working_dir/allowed_tools/permission_mode/resume) 完整
   - 会话管理方法测试 (set_session_id/get_session_id) 完整
   - 工具跟踪逻辑测试 (_track_tool_use/_extract_bash_file_paths) 覆盖 Write/Edit/Bash 等工具
   - run() 方法测试 (收集 TEXT 消息/处理 ERROR/提取 metadata) 使用 Mock 完整
   - run_stream() 方法测试 (处理 AssistantMessage/ResultMessage/回调函数) 使用 Mock SDK 完整
   - 环境变量处理测试 (CLAUDECODE 清除和恢复) 完整
   - 会话完整性延迟配置测试 (SESSION_WRITE_DELAY_SECONDS=20) 完整

### T1.8 任务接口集成测试与修复

**执行角色**: 技术经理

- [x] 检查并修复 POST /api/task 接口调用测试
- [x] 检查并修复 SSE 流式输出完整性测试
- [x] 检查并修复工具使用跟踪功能测试

**检查结果**: 测试文件完整，无需修复

测试文件: `tests/test_task_api.py` (共 1663 行，15 个测试类，80+ 测试用例)

1. **POST /api/task 接口调用测试** (TestPostTaskEndpoint):
   - TaskRequest 模型验证 (prompt/working_dir/tools/resume/continue_conversation/new_session/permission_mode)
   - TaskResponse 模型验证 (success/message/session_id/cost_usd/duration_ms/files_changed/tools_used)
   - 成功执行任务测试
   - 任务执行失败测试
   - 自定义工作目录测试

2. **SSE 流式输出完整性测试** (TestSSESteamEndpoint):
   - 流式消息类型完整性测试 (7 种消息类型: text/tool_use/tool_result/thinking/error/complete/ask_user_question)
   - 流式消息结构测试 (StreamMessage 所有字段)
   - SSE 事件生成器结构测试 (StreamingResponse/media_type/Cache-Control)
   - SSE 事件内容格式测试 (TEXT/TOOL_USE/TOOL_RESULT/ERROR/COMPLETE 消息)
   - 工具调用消息处理测试
   - 错误消息处理测试

3. **工具使用跟踪功能测试** (TestToolUsageTracking):
   - 基本工具跟踪测试 (Read 工具)
   - 工具去重测试 (同一工具多次调用只记录一次)
   - Write/Edit 工具文件跟踪测试
   - Bash 重定向命令文件跟踪测试 (> 和 >>)
   - Bash tee 命令文件跟踪测试
   - Bash touch 命令文件跟踪测试
   - Bash rm 命令文件跟踪测试
   - Bash mv 命令文件跟踪测试
   - Bash cp 命令文件跟踪测试
   - mkdir 不跟踪测试 (目录不是文件)
   - 带引号的文件路径测试
   - 未知工具处理测试
   - 空文件路径处理测试
   - _extract_bash_file_paths 全面覆盖测试

**额外测试覆盖**:
- 会话管理测试 (TestSessionManagement): 创建/获取/设置等待/移除/列出会话
- API 端点参数测试 (TestAPIEndpointParameters): 默认值/权限模式/问答请求/新会话请求
- 辅助函数测试 (TestHelperFunctions): get_project_dir_name/get_latest_session_id
- 会话恢复流程测试 (TestSessionRecovery): check_session_file_valid/会话文件验证
- SessionManager 状态转换测试 (TestSessionManagerStateTransitions): 合法/非法状态转换
- 上下文管理测试 (TestContextManagement): 客户端引用/状态变化保持
- 多轮会话测试 (TestMultiTurnSession): continue_conversation/会话ID持久化/多会话独立
- 会话中断后恢复测试 (TestSessionRecoveryAfterInterruption): 有效/无效会话文件恢复
- 超时处理测试 (TestTimeoutHandling): 会话总超时/活动超时/清理过期会话

---

## T2 - 会话历史管理

**功能模块**: 会话历史管理、历史记录选择、会话列表

### T2.1 会话历史存储需求检查与修复

**执行角色**: 产品经理

- [x] 检查并修复会话历史的存储需求
- [x] 检查并修复历史记录的查询和展示需求
- [x] 检查并修复历史记录与当前会话的关联关系

**检查结果**: ✅ 需求文档完整，无需修复

1. **会话历史的存储需求**：
   - 存储位置: `~/.claude/projects/{encoded-project-path}/{sessionId}.jsonl` (完整定义)
   - 文件格式: JSONL 格式，每行一个 JSON 对象 (完整定义)
   - 消息类型: queue-operation, file-history-snapshot, user, assistant (完整定义)
   - 消息链结构: 通过 uuid 和 parentUuid 构成链式结构 (完整定义)
   - 会话保留策略: 保留最近 30 天的会话数据 (完整定义)

2. **历史记录的查询和展示需求**：
   - 项目列表 API: GET /api/projects (完整定义)
   - 会话列表 API: GET /api/sessions (完整定义，包含分页和排序参数)
   - 会话标题生成规则: 标题来源、标签过滤、长度限制、空内容处理 (完整定义)
   - 排序规则: timestamp/title 两种排序方式 (完整定义)
   - 分页规则: limit/offset 配合使用，返回 total 字段 (完整定义)

3. **历史记录与当前会话的关联关系**：
   - 数据流向图: jsonl 文件 → 历史记录页面 / 当前会话页面 (完整定义)
   - continue_conversation: 通过 API 获取最近会话 (完整定义)
   - resume: 直接读取 jsonl 文件恢复 (完整定义)
   - 会话数据回填规则: 会话ID、工作目录、历史消息 (完整定义)
   - 消息解析规则: content 类型处理、标签过滤 (完整定义)

**参考文档**:
- `docs/需求文档/历史记录-需求.md` - 历史记录管理需求
- `docs/需求文档/当前会话功能/当前会话-会话格式-需求.md` - 会话文件格式定义
- `docs/需求文档/当前会话功能/当前会话-继续会话-需求.md` - 继续会话详细需求
- `docs/需求文档/当前会话功能/当前会话-历史记录选择-需求.md` - 历史记录选择需求

### T2.2 会话列表需求检查与修复

**执行角色**: 产品经理

- [x] 检查并修复会话列表的分页和排序需求
- [x] 检查并修复会话标题和摘要的生成规则需求

**检查结果**: ✅ 需求文档完整，无需修复

1. **会话列表的分页和排序需求**：
   - 分页参数定义完整：limit（默认 20，最大 100）、offset（默认 0）
   - 分页行为描述清晰：`limit` 和 `offset` 配合使用实现分页，排序在分页之前执行
   - 排序参数定义完整：sort_by（timestamp/title）、order（asc/desc）
   - 排序行为说明详细：timestamp 按创建时间排序，title 按标题排序（空标题排最后）
   - 响应包含 total 字段，用于计算总页数
   - 文档位置：`docs/需求文档/历史记录-需求.md` 第 134-156 行

2. **会话标题和摘要的生成规则需求**：
   - 标题生成规则完整（第 40-67 行）：标题来源、content 类型处理、标签过滤、长度限制（100字符）、空内容处理、标题提取流程
   - 摘要生成规则完整（第 69-96 行）：摘要来源、content 类型处理、标签过滤、长度限制（30字符）、空内容处理、摘要提取流程
   - 标题与摘要差异对比表清晰（第 99-107 行）：用途、长度限制、空值默认、API 返回字段
   - 标签过滤规则明确：移除 `<ide_selection>` 和 `<ide_opened_file>` 标签及其内容
   - content 类型处理完整：字符串类型直接使用，数组类型遍历提取 text 类型内容块

**参考文档**:
- `docs/需求文档/历史记录-需求.md` - 历史记录管理需求（第 3.2.1、3.2.2、3.3.2 节）

### T2.3 Session 数据模型设计检查与修复

**执行角色**: 架构师

- [x] 检查并修复 Session 数据模型设计完整性
- [x] 检查并修复会话状态字段设计
- [x] 检查并修复路径编解码的设计方案

**检查结果**:
1. **Session 数据模型设计完整性**: ⚠️ 基本符合，但存在模型冗余和字段不一致问题
   - 同时存在 `SessionStateData` 和 `SessionState` 两个模型，造成维护负担
   - `SessionStateData` 使用 `state: SessionStateEnum`，但 `SessionInfo` 仍使用 `is_waiting: bool`
   - 修复建议：统一使用 `SessionStateEnum`，逐步移除 `SessionState` 模型

2. **会话状态字段设计**: ✅ 符合设计规范
   - 6种状态枚举定义完整（pending/idle/running/waiting/completed/failed）
   - 状态转换逻辑正确，状态机实现完整
   - 状态优先级定义正确（running > waiting > pending > completed > failed > idle）

3. **路径编解码的设计方案**: ❌ 存在可逆性问题，需要修复
   - 当前算法无法还原包含 `-` 或 `_` 的原始路径
   - 修复建议：改进编码算法，使用字符转义（`~2d` 和 `~5f`）确保可逆性
   - 另：`get_project_dir_name()` 函数在 task.py 和 session.py 中重复定义，应移至公共模块

**修复优先级**:
- P0: 路径编解码不可逆问题
- P1: 状态字段不一致、模型冗余
- P2: 函数重复定义

### T2.4 会话历史 API 设计检查与修复

**执行角色**: 架构师

- [x] 检查并修复会话历史 API 路由设计
- [x] 检查并修复分页和排序设计

**检查结果**: ⚠️ 基本符合，发现以下问题需修复

1. **会话历史 API 路由设计**: ⚠️ 基本符合
   - `/api/sessions` 接口缺少 `summary` 字段（需求文档第 164 行要求）
   - 前端调用 `/api/sessions` 时未传递排序参数
   - 分页参数风格不一致（offset vs page）

2. **分页和排序设计**: ⚠️ 基本符合
   - 分页响应缺少 `pages` 字段（总页数）
   - 空标题排序逻辑不完全正确（order=desc 时空标题可能不在最后）
   - 缺少排序参数校验（sort_by、order 有效值校验）

**修复优先级**:
- **P0**: 添加 `summary` 字段（影响下拉选择等功能）
- **P1**: 统一分页响应格式、修正空标题排序逻辑
- **P2**: 添加排序参数校验、统一分页风格

**修复位置**:
- `app/routers/session.py` - 添加 summary 字段、修正排序逻辑、统一分页响应
- `web/static/components/historyDrawer.js` - 前端调用添加排序参数

### T2.5 Session 模型代码检查与修复

**执行角色**: 全栈工程师

- [x] 检查并修复 Session 模型字段完整性（id、title、status、messages、created_at 等）
- [x] 检查并修复会话历史查询逻辑
- [x] 检查并修复时间戳处理逻辑

**检查结果**: ✅ 基本完整，发现并修复了分页响应格式问题

1. **Session 模型字段完整性**: ✅ 符合规范
   - `SessionStateData` 包含完整字段：`session_id`, `client`, `state`, `created_at`, `last_activity`, `cwd`, `pending_question_id`
   - `SessionInfo` 字段完整（用于 API 返回）：`session_id`, `is_waiting`, `pending_question_id`, `created_at`, `last_activity`, `cwd`
   - `parse_session_metadata` 返回字段完整：`id`, `title`, `summary`, `timestamp`, `message_count`, `size_bytes`, `tools`, `cwd`, `has_question`, `question_timestamp`
   - 时间戳使用 Unix 时间戳（`time.time()`）存储，足够精确

2. **会话历史查询逻辑**: ✅ 基本正确，已修复分页响应格式
   - `/api/sessions` 接口已返回 `summary` 字段（`parse_session_metadata` 生成）
   - 标题和摘要生成逻辑正确：标签过滤（`<ide_selection>`, `<ide_opened_file>`）、长度限制（标题100字符，摘要30字符）、空值处理
   - 排序逻辑正确：按时间戳/标题排序，空值排在最后
   - **修复**: 分页响应添加 `pages` 字段（总页数），符合需求文档要求

3. **时间戳处理逻辑**: ✅ 正确
   - 会话创建使用 `time.time()` 获取系统时间（第 116-117 行）
   - 元数据解析使用 jsonl 文件中的 ISO 8601 时间戳（第 217-218 行）
   - 超时判断使用系统时间差计算（第 361-362 行）
   - 活动时间更新正确（第 256, 295, 326 行）

**修复内容**:
- `app/routers/session.py` 第 390-395 行：`/api/sessions` 接口响应添加 `pages` 字段

**代码位置**:
- 会话模型定义：`app/routers/session_manager.py` 第 109-163 行
- 会话元数据解析：`app/routers/session.py` 第 150-294 行
- 会话列表 API：`app/routers/session.py` 第 316-397 行
- 测试文件：`tests/test_session.py`（981 行，覆盖 Session 模型、状态机、API 接口）

### T2.6 路径与排序代码检查与修复

**执行角色**: 全栈工程师

- [x] 检查并修复路径编解码处理逻辑
- [x] 检查并修复会话列表排序逻辑
- [x] 检查并修复会话删除逻辑

**检查结果**: 已完成修复

1. **路径编解码处理逻辑**: 已修复
   - **问题**: 原编码算法将 `/`、`\`、`_` 都转换为 `-`，导致解码时无法区分原始字符
   - **修复方案**: 使用字符转义确保可逆性
     - `~` → `~7e`（先转义转义符本身）
     - `-` → `~2d`
     - `_` → `~5f`
     - 然后将 `/` 和 `\` 转换为 `-`
   - **新增公共模块**: `app/services/path_utils.py`
     - `get_project_dir_name()`: 编码项目路径为目录名
     - `decode_project_name()`: 解码目录名为原始路径
     - 兼容旧版本的别名函数

2. **会话列表排序逻辑**: 检查确认已正确实现
   - 按标题排序时：空标题（"无标题"）始终排在最后，无论 order 是 asc 还是 desc
   - 按时间戳排序时：无时间戳的会话排在最后
   - 实现逻辑：分离空值和非空值，分别排序后合并

3. **会话删除逻辑**: 已增强
   - **修复内容**: 删除会话时同步清理内存中的活跃会话
   - **修改位置**: `app/routers/session.py` 的 `delete_session()` 函数
   - **逻辑**: 删除会话文件 → 使缓存失效 → 调用 `session_manager.remove_session()`

4. **重复代码消除**:
   - 将 `task.py` 和 `session.py` 中重复的 `get_project_dir_name()` 函数移至公共模块 `app/services/path_utils.py`
   - 更新 `app/services/__init__.py` 导出新函数
   - 两个路由文件现在都从公共模块导入

**代码位置**:
- 路径编解码公共模块: `app/services/path_utils.py`
- 会话列表排序: `app/routers/session.py` 第 315-343 行
- 会话删除: `app/routers/session.py` 第 933-976 行

### T2.7 Session 模型测试与修复

**执行角色**: 技术经理

- [x] 检查并修复 Session 模型序列化测试
- [x] 检查并修复路径编解码工具函数测试
- [x] 检查并修复会话状态枚举测试

**检查结果**: ✅ 测试完整，无需修复

1. **Session 模型序列化测试**: 所有字段正确序列化/反序列化
   - SessionState: 6 个测试用例，覆盖字段完整性、创建、工作目录、等待状态、时间戳
   - SessionInfo: 4 个测试用例，覆盖字段完整性、创建、工作目录
   - SessionStateData: 6 个测试用例，覆盖字段完整性、创建、状态属性

2. **路径编解码工具函数测试**: 编码算法可逆，特殊字符处理正确
   - TestDecodeProjectName: 6 个测试用例
   - TestGetProjectDirName: 11 个测试用例
   - TestPathUtilsModule: 12 个测试用例
   - 可逆性验证: 简单路径、带下划线、带连字符、带波浪号、复杂路径全部通过

3. **会话状态枚举测试**: 状态转换逻辑正确，优先级定义合理
   - TestSessionStateEnum: 5 个测试用例（枚举值、数量、字符串转换、兼容性、无效值处理）
   - TestSessionStateMachine: 8 个测试用例（转换表、合法/非法转换、状态获取）
   - TestStatePriority: 4 个测试用例（优先级值、顺序、全覆盖）

### T2.8 会话历史接口测试与修复

**执行角色**: 技术经理

- [x] 检查并修复会话历史 API 接口测试
- [x] 检查并修复历史记录分页和排序测试
- [x] 检查并修复会话删除功能测试

**检查结果**: 测试文件已完善，新增以下测试用例

测试文件: `tests/test_session.py` (共 1200+ 行)

1. **会话历史 API 接口测试** (TestListSessionsAPI):
   - 新增 `test_list_sessions_response_contains_summary_field`: 验证 `/api/sessions` 接口响应包含 `summary` 字段（T2.4 检查项）
   - 新增 `test_list_sessions_response_contains_pages_field`: 验证分页响应包含 `pages` 字段（T2.4 检查项）
   - 原有测试: 空工作目录、不存在目录、基本列表返回

2. **历史记录分页和排序测试** (TestSessionsPaginationAndSorting):
   - 新增 `test_pagination_pages_field`: 验证分页 pages 字段计算正确性
   - 新增 `test_sort_by_title_empty_title_last`: 验证空标题排序逻辑（order=desc/asc 时空标题始终在最后）
   - 新增 `test_sort_parameters_validation`: 验证排序参数（sort_by、order）校验
   - 原有测试: limit/offset 分页、时间戳升序/降序排序、标题排序、总数统计

3. **会话删除功能测试** (TestDeleteSession):
   - 新增 `test_delete_session_response_format`: 验证删除会话响应格式完整性
   - 原有测试: 删除不存在会话、成功删除会话、删除后文件移除

**API 实现确认**:
- `/api/sessions` 接口已正确返回 `summary` 字段（`parse_session_metadata` 生成，第 222 行）
- `/api/sessions` 接口已正确返回 `pages` 字段（第 353-360 行）
- 空标题排序逻辑正确：空标题（"无标题"）始终排在最后（第 316-330 行）
- 排序参数使用 FastAPI Query 默认值，宽松验证策略

---

## T3 - 会话状态与恢复

**功能模块**: 会话恢复、多轮会话、任务状态管理

### T3.1 会话恢复需求检查与修复

**执行角色**: 产品经理

- [x] 检查并修复会话恢复的触发条件和场景
- [x] 检查并修复多轮会话的上下文保持需求
- [x] 检查并修复会话超时间和清理策略需求

**检查结果**: ✅ 需求文档完整，无需修复

1. **会话恢复的触发条件和场景**: 完整定义
   - 两种恢复方式：continue_conversation（延续最近会话）和 resume（恢复指定会话）
   - 触发方式明确：勾选复选框 / 从历史记录选择
   - 数据来源明确：最近一次会话 / 用户选择的指定会话
   - 数据回填规则完整：会话ID、工作目录、历史消息
   - 参考文档：`当前会话-继续会话-需求.md` 第 2-4 节

2. **多轮会话的上下文保持需求**: 完整定义
   - 消息链机制：通过 `parentUuid` 关联形成链式结构
   - 上下文累积传递：每次回复都能看到之前的对话历史
   - 多轮会话判断规则：`permissionMode` 字段存在 → 新会话开始；`parentUuid` 为 null → 首条用户消息；内容包含 `tool_result` → 继续当前对话
   - 恢复时消息历史重建：从 jsonl 文件按 `parentUuid` 链重建完整消息历史
   - 参考文档：`当前会话-主需求.md` 第 5.3 节、`当前会话-会话格式-需求.md` 第 6 节

3. **会话超时间和清理策略需求**: 完整定义
   - 超时类型：单次答案超时 (300秒)、会话总超时 (3600秒)、活动超时 (1800秒)
   - 超时处理流程：检测 → 发送 error 消息 → 清理资源 → 关闭 SSE 连接
   - 清理触发条件：任务完成/错误、用户点击新会话、会话超时、活动超时、服务重启、页面关闭
   - 资源清理规则：内存会话状态、SSE 连接、jsonl 文件（30天超期）、客户端实例
   - 参考文档：`当前会话-会话状态.md` 第 5-7 节、`当前会话-主需求.md` 第 6.7.4/6.7.7 节

**参考文档**:
- `docs/需求文档/当前会话功能/当前会话-继续会话-需求.md` - 会话恢复详细需求
- `docs/需求文档/当前会话功能/当前会话-会话状态.md` - 超时处理和清理策略
- `docs/需求文档/当前会话功能/当前会话-会话格式-需求.md` - 消息链结构
- `docs/需求文档/当前会话功能/当前会话-概念辨析.md` - 多轮会话 vs 继续会话概念辨析

### T3.2 会话状态定义需求检查与修复

**执行角色**: 产品经理

- [x] 检查并修复会话状态（pending/running/completed/failed）的定义
- [x] 检查并修复任务中断和续接的需求

**检查结果**: ✅ 需求文档完整，无需修复

1. **会话状态定义完整性**: 6种状态完整定义（pending/idle/running/waiting/completed/failed）
   - 状态优先级定义完整：running > waiting > pending > completed > failed > idle
   - 包含与旧版状态名称的对应关系说明（executing → running, error → failed）
   - 定义位置：`当前会话-会话状态.md` 第 2.2 节

2. **状态转换条件明确性**: 转换条件和触发时机完整
   - 生命周期转换条件完整（创建→初始化→pending→running→completed/failed）
   - SSE 消息与状态转换关联清晰（9种转换场景）
   - 定义位置：`当前会话-会话状态.md` 第 3-4 节、`当前会话-主需求.md` 第 6.5.3.2 节

3. **任务中断场景完整性**: 7种中断场景完整定义
   - 用户主动中断：同步任务中断、流式任务中断、问答中断
   - 超时中断：答案超时(300秒)、会话总超时(3600秒)、活动超时(1800秒)
   - 错误码与状态转换关联明确（7种error_code）
   - 定义位置：`当前会话-主需求.md` 第 5.5.1 节、第 6.7.4-6.7.6 节

4. **任务续接需求完整性**: 3种续接类型完整定义
   - 问答续接：用户提交答案后继续执行（POST /api/task/answer）
   - 会话恢复：恢复历史会话继续执行（resume 参数）
   - 继续会话：延续最近会话上下文（continue_conversation=true）
   - 定义位置：`当前会话-主需求.md` 第 5.5.2 节、`当前会话-继续会话-需求.md`

5. **数据恢复需求明确性**: 清理策略和数据恢复规则完整
   - 清理触发条件完整（7种场景）
   - 资源清理规则明确（内存状态、SSE连接、jsonl文件、客户端实例）
   - 已接收数据处理规则明确（保留、去重、顺序保证、状态一致性）
   - 定义位置：`当前会话-会话状态.md` 第 7 节、`当前会话-主需求.md` 第 6.5.3.1 节

**参考文档**:
- `docs/需求文档/当前会话功能/当前会话-会话状态.md` - 会话状态核心定义
- `docs/需求文档/当前会话功能/当前会话-主需求.md` - 主需求文档
- `docs/需求文档/当前会话功能/当前会话-继续会话-需求.md` - 会话恢复需求

### T3.3 状态转换设计检查与修复

**执行角色**: 架构师

- [x] 检查并修复会话状态转换设计
- [x] 检查并修复消息历史存储结构设计
- [x] 检查并修复上下文传递的设计方案

**检查结果**:

1. **会话状态转换设计**: ✅ 符合设计规范

   - **状态枚举定义完整**: 代码实现了 6 种状态（pending/idle/running/waiting/completed/failed），与需求文档 `当前会话-会话状态.md` 定义完全一致
   - **状态机实现正确**: `SessionStateMachine` 类定义了完整的状态转换表 `TRANSITIONS`，涵盖所有合法转换路径
   - **状态优先级正确**: `STATE_PRIORITY` 字典定义正确（running > waiting > pending > completed > failed > idle）
   - **状态转换方法完善**: `transition_state()` 方法通过事件触发状态转换，`set_waiting()` 兼容旧接口
   - **代码位置**: `app/routers/session_manager.py` 第 18-106 行

2. **消息历史存储结构设计**: ✅ 符合设计规范

   - **会话文件格式正确**: 使用 JSONL 格式存储，每行一个 JSON 对象，与需求文档 `当前会话-会话格式-需求.md` 定义一致
   - **消息类型完整**: 支持 user/assistant 消息类型，content 支持文本、thinking、tool_use、tool_result
   - **消息链结构完整**: 通过 `uuid` 和 `parentUuid` 构建链式结构，支持多轮对话
   - **元数据缓存机制**: `MetadataCache` 类实现了会话元数据缓存，基于文件修改时间判断缓存有效性
   - **会话解析完整**: `parse_session_metadata()` 和 `parse_content_blocks()` 函数完整解析会话内容
   - **代码位置**: `app/routers/session.py` 第 28-256 行（缓存）、496-594 行（内容解析）

3. **上下文传递的设计方案**: ✅ 符合设计规范

   - **会话恢复机制完善**:
     - `continue_conversation`: 通过 `get_latest_session_id()` 获取最近会话
     - `resume`: 直接使用指定的会话 ID
     - `can_resume` 标志正确判断会话可恢复性（检查内存会话和文件有效性）
   - **SDK 上下文传递正确**: `ClaudeCodeClient` 通过 `resume` 参数将上下文传递给 SDK
   - **会话 ID 管理正确**:
     - 后端生成 UUID 作为会话 ID
     - SDK 返回的 `session_id` 会被记录（在 complete 消息的 metadata 中）
     - `effective_session_id` 优先使用 SDK 返回的 ID
   - **多轮对话支持完整**:
     - 会话保留机制：任务完成后保留会话，支持后续对话
     - 消息历史加载：`/api/sessions/{session_id}/messages` 接口返回完整消息历史
   - **代码位置**:
     - `app/routers/task.py` 第 341-413 行（会话恢复逻辑）
     - `app/claude_runner/client.py` 第 134-146 行（resume 参数）
     - `app/routers/session.py` 第 597-695 行（消息历史 API）

**额外发现**:

1. **状态字段设计建议（低优先级）**:
   - 当前同时存在 `SessionStateData` 和 `SessionState` 两个模型，`SessionState` 仅为兼容旧接口
   - `SessionInfo` 使用 `is_waiting: bool` 而非 `state: SessionStateEnum`
   - 建议：后续版本统一使用 `SessionStateEnum`，逐步移除 `SessionState` 模型

2. **会话完整性延迟设计（已实现）**:
   - `SESSION_WRITE_DELAY_SECONDS = 20` 确保会话记录完整写入
   - 避免会话停留在 "thinking" 阶段的问题
   - 代码位置: `app/claude_runner/client.py` 第 32 行、424-432 行

**参考文档**:
- `docs/需求文档/当前会话功能/当前会话-会话状态.md` - 会话状态核心定义
- `docs/需求文档/当前会话功能/当前会话-会话格式-需求.md` - 会话文件格式定义
- `docs/需求文档/当前会话功能/当前会话-继续会话-需求.md` - 会话恢复需求
- `docs/需求文档/当前会话功能/当前会话-消息类型.md` - 消息类型定义

### T3.4 恢复机制设计检查与修复

**执行角色**: 架构师

- [x] 检查并修复恢复机制的实现方案
- [x] 检查并修复任务队列的设计（如有）

**检查结果**: ✅ 核心实现与需求文档一致，发现5个优化建议

1. **恢复机制实现方案**: ✅ 符合设计规范
   - 会话恢复机制完整：支持 `continue_conversation`（最近会话）和 `resume`（指定会话）两种方式
   - 会话有效性验证完整：文件存在性、非空、必要字段、sessionId 匹配验证
   - 会话完整性延迟：20秒延迟确保会话记录完整写入
   - 代码位置：`app/routers/task.py` 第 346-399 行

2. **任务队列设计**: ✅ 符合设计规范
   - 任务队列存储完整：使用 JSON 文件存储，支持 add/pop/remove/clear 操作
   - 任务恢复机制：服务重启后未完成任务重新排队（`_recover_running_tasks`）
   - 数据一致性修复：自动修复重复 ID 和时间格式问题
   - 代码位置：`app/scheduler/storage_queue.py`、`app/scheduler/scheduler.py`

3. **发现的设计优化建议**:
   - P1（中）：`get_latest_session_id()` 建议增加 `cwd` 字段验证，避免跨项目会话恢复
   - P2（低）：内存会话与文件会话状态不同步，服务重启后等待状态丢失
   - P3（低）：会话恢复并发处理不足，建议增加会话级别锁机制
   - P4（中）：任务恢复缺少进度保存，长时间任务中断后需重新执行
   - P5（低）：队列任务无优先级区分

4. **实现与需求文档一致性**:
   - 会话恢复机制：与 `当前会话-继续会话-需求.md` 一致
   - 会话状态管理：与 `当前会话-会话状态.md` 一致
   - 状态转换规则：与 `当前会话-技术设计.md` 一致
   - 超时处理：与需求文档一致（会话总超时 3600 秒，活动超时 1800 秒）

### T3.5 状态转换代码检查与修复

**执行角色**: 全栈工程师

- [x] 检查并修复会话状态转换逻辑
- [x] 检查并修复任务 ID 生成和关联逻辑
- [x] 检查并修复恢复时会话历史重建逻辑

**检查结果**: ⚠️ 发现并修复了状态转换逻辑不完整的问题

1. **会话状态转换逻辑**: ⚠️ 发现问题并已修复
   - **问题**: 状态机定义了完整的状态转换（task_start/task_complete/task_error），但 `task.py` 只使用了 `set_waiting` 方法
   - **影响**: 任务开始时没有 PENDING→RUNNING 转换，完成时没有 RUNNING→COMPLETED 转换，错误时没有转换直接删除会话
   - **修复内容**:
     - 任务开始时添加 `transition_state(session_id, "task_start")` 调用 (PENDING → RUNNING)
     - COMPLETE 消息处理时检查 `is_error` 标志，区分正常完成和错误完成
     - 正常完成时调用 `transition_state(session_id, "task_complete")` (RUNNING → COMPLETED)
     - 错误完成时调用 `transition_state(session_id, "task_error")` (RUNNING/WAITING → FAILED)
     - 异常处理时先转换状态再删除会话
   - **修复位置**: `app/routers/task.py` 第 428-430 行（任务开始）、540-558 行（任务完成/错误）

2. **任务 ID 生成和关联逻辑**: ✅ 正确实现
   - **会话 ID 生成**: 使用 `uuid.uuid4()` 生成唯一 ID，符合需求文档定义
   - **SDK session_id 关联**:
     - 后端先生成 `session_id`，然后通过 `client.set_session_id()` 设置
     - SDK 在 COMPLETE 消息的 metadata 中返回 `session_id`
     - 使用 `effective_session_id = sdk_session_id or session_id` 优先使用 SDK 返回的 ID
   - **effective_session_id 使用**: 正确用于 SSE 消息的 session_id 字段
   - **代码位置**: `app/routers/task.py` 第 390-405 行（ID 生成）、439-447 行（SDK ID 关联）

3. **恢复时会话历史重建逻辑**: ✅ 正确实现
   - **continue_conversation**: 通过 `get_latest_session_id(cwd)` 获取最近会话 ID
   - **resume**: 直接使用用户指定的会话 ID
   - **会话文件验证**: `check_session_file_valid()` 验证文件存在性、非空、必要字段、sessionId 匹配
   - **消息历史加载**: `/api/sessions/{session_id}/messages` 接口从 jsonl 文件读取消息
   - **消息解析**: `parse_content_blocks()` 支持 text/thinking/tool_use/tool_result 四种类型
   - **分页支持**: 支持 page/limit 参数分页加载，优化大文件性能
   - **代码位置**:
     - `app/routers/task.py` 第 350-388 行（会话恢复逻辑）
     - `app/routers/session.py` 第 597-695 行（消息历史 API）
     - `app/routers/session.py` 第 496-594 行（内容块解析）

**状态机转换表验证**:
| 转换路径 | 状态机定义 | 代码实现 | 状态 |
|---------|----------|---------|------|
| 创建会话 | None → PENDING | `create_session()` 初始状态为 PENDING | ✅ |
| 开始任务 | PENDING → RUNNING | 新增 `transition_state("task_start")` | ✅ 已修复 |
| 问答等待 | RUNNING → WAITING | `set_waiting(True)` 内部调用状态机 | ✅ |
| 提交答案 | WAITING → RUNNING | `set_waiting(False)` 内部调用状态机 | ✅ |
| 正常完成 | RUNNING → COMPLETED | 新增 `transition_state("task_complete")` | ✅ 已修复 |
| 错误完成 | RUNNING/WAITING → FAILED | 新增 `transition_state("task_error")` | ✅ 已修复 |
| 清理资源 | COMPLETED/FAILED → IDLE | `cleanup_old_sessions()` 调用 `cleanup` 事件 | ✅ |

### T3.6 上下文与超时代码检查与修复

**执行角色**: 全栈工程师

- [x] 检查并修复多轮对话上下文累积逻辑
- [x] 检查并修复超时处理逻辑

**检查结果**:

1. **多轮对话上下文累积逻辑检查**: ✅ 符合设计规范

   - **continue_conversation 参数处理**: 正确实现
     - `task.py` 第 350-358 行：检查 `continue_conversation=True` 时调用 `get_latest_session_id()` 获取最近会话
     - 正确处理无最近会话的情况（创建新会话）

   - **resume 参数处理**: 正确实现
     - `task.py` 第 365-388 行：正确处理会话恢复逻辑
     - 检查内存会话存在性 (`existing_session`)
     - 检查会话文件有效性 (`check_session_file_valid`)
     - 正确设置 `can_resume` 标志

   - **消息历史从 jsonl 文件加载**: 正确实现
     - `session.py` 第 597-695 行：`/api/sessions/{session_id}/messages` 接口正确解析 jsonl 文件
     - `parse_content_blocks()` 函数正确处理字符串和数组类型的 content
     - 支持 text/thinking/tool_use/tool_result 四种内容块类型

   - **上下文传递给 SDK**: 正确实现
     - `client.py` 第 139-146 行：`resume` 参数正确传递给 SDK
     - `task.py` 第 394-402 行：只有 `can_resume=True` 时才传递 `resume` 参数

   - **多轮会话消息链完整性**: 正确实现
     - `session.py` 第 643-665 行：消息解析保留 uuid、parentUuid 字段
     - 支持 permissionMode 字段用于识别新会话轮次

2. **超时处理逻辑检查**: ⚠️ 发现问题并已修复

   - **问题1**: 单次答案超时 (300秒) 未实现
     - 需求文档定义：单次答案超时 300 秒 (5 分钟)
     - 原实现：`cleanup_old_sessions()` 只有 `max_age_seconds` 和 `activity_timeout` 参数
     - 修复：添加 `answer_timeout` 参数，专门检测等待状态会话的超时
     - 修复位置：`session_manager.py` 第 352-427 行

   - **问题2**: 超时清理没有被定期调用
     - 原实现：`cleanup_old_sessions()` 方法存在但没有被定期调用
     - 修复：在 `main.py` 添加 `session_cleanup_loop()` 后台任务，每 60 秒执行一次清理
     - 修复位置：`main.py` 第 59-103 行（清理循环）、121-133 行（生命周期管理）

   - **问题3**: 超时后未发送通知
     - 原实现：清理后只记录日志
     - 修复：`cleanup_old_sessions()` 返回详细的清理信息（包含 timeout_reason）
     - 后续优化：未来可添加 SSE 通知机制

3. **超时配置参数**（符合需求文档 `当前会话-会话状态.md` 第 5.1 节）:

   | 超时类型 | 时间限制 | 实现状态 |
   |---------|---------|---------|
   | 单次答案超时 | 300 秒 (5 分钟) | ✅ 已修复 |
   | 会话总超时 | 3600 秒 (1 小时) | ✅ 已实现 |
   | 活动超时 | 1800 秒 (30 分钟) | ✅ 已实现 |

4. **测试覆盖**:

   - 更新 `tests/test_task_api.py` 中的 `TestTimeoutHandling` 测试类
   - 添加 `test_cleanup_answer_timeout_waiting_session` 测试用例
   - 添加 `test_cleanup_answer_timeout_not_waiting_session` 测试用例
   - 更新现有测试以适应新的返回类型（`list[dict]` 而非 `int`）

**修复位置**:
- `app/routers/session_manager.py` 第 352-427 行：`cleanup_old_sessions()` 方法增强
- `app/main.py` 第 44-135 行：会话清理后台任务和生命周期管理
- `tests/test_task_api.py` 第 1460-1663 行：超时处理测试更新

### T3.7 状态转换测试与修复

**执行角色**: 技术经理

- [x] 检查并修复会话状态枚举和转换测试
- [x] 检查并修复会话恢复流程测试
- [x] 检查并修复上下文管理测试

**检查结果**: ✅ 测试覆盖完整，已补充缺失测试

1. **会话状态枚举和转换测试**: ✅ 原有测试完整，补充了以下测试用例
   - 原有测试覆盖：`TestSessionStateEnum`（5个）、`TestSessionStateMachine`（8个）、`TestStatePriority`（4个）、`TestSessionManagerStateTransitions`（5个）
   - 新增 `TestStateTransitionCompleteness` 测试类（6个测试用例）：
     - `test_waiting_to_timeout_transition`: WAITING -> timeout -> FAILED 转换
     - `test_session_manager_timeout_transition`: SessionManager 的 timeout 事件转换
     - `test_pending_session_timeout_cleanup`: PENDING 状态会话的超时清理
     - `test_multiple_consecutive_transitions`: 多次连续状态转换的正确性
     - `test_error_transition_from_running`: 从 RUNNING 状态出错转换
     - `test_error_transition_from_waiting`: 从 WAITING 状态出错转换

2. **会话恢复流程测试**: ✅ 原有测试完整，补充了以下测试用例
   - 原有测试覆盖：`TestSessionRecovery`（10个）、`TestSessionRecoveryAfterInterruption`（8个）
   - 新增 `TestSessionRecoveryCompleteness` 测试类（6个测试用例）：
     - `test_continue_conversation_and_resume_mutual_exclusivity`: 参数互斥性验证
     - `test_can_resume_flag_with_valid_session`: 有效会话的 can_resume 标志
     - `test_can_resume_flag_with_memory_session`: 内存会话存在时的 can_resume 标志
     - `test_resume_with_cwd_from_session_file`: 恢复会话时从会话文件获取工作目录
     - `test_continue_conversation_without_history`: 无历史会话时的 continue_conversation 行为
     - `test_session_file_with_multiple_messages`: 包含多条消息的会话文件恢复

3. **上下文管理测试**: ✅ 原有测试完整，补充了以下测试用例
   - 原有测试覆盖：`TestContextManagement`（2个）、`TestMultiTurnSession`（7个）
   - 新增 `TestContextManagementCompleteness` 测试类（6个测试用例）：
     - `test_client_resume_parameter_passing`: 客户端 resume 参数正确传递
     - `test_session_id_persistence_across_states`: 会话 ID 在状态变化中的持久化
     - `test_context_preserved_on_answer_submission`: 提交答案后上下文保持
     - `test_multiple_sessions_context_isolation`: 多个会话上下文隔离
     - `test_session_cwd_management`: 会话工作目录管理
     - `test_pending_question_id_tracking`: 待处理问题 ID 跟踪

**测试位置**:
- 状态机实现：`app/routers/session_manager.py` 第 18-106 行
- 状态转换测试：`tests/test_session.py` 第 354-517 行（原有）、`tests/test_task_api.py` 第 1771-1859 行（新增）
- 会话恢复测试：`tests/test_task_api.py` 第 762-961 行（原有）、第 1862-1967 行（新增）
- 上下文管理测试：`tests/test_task_api.py` 第 1083-1119 行（原有）、第 1970-2073 行（新增）

**改进建议**:
- P2（低）：建议在 `TaskRequest` 模型中添加验证器确保 `continue_conversation` 和 `resume` 互斥性

### T3.8 多轮会话测试与修复

**执行角色**: 技术经理

- [x] 检查并修复多轮会话连续执行测试
- [x] 检查并修复会话中断后恢复测试
- [x] 检查并修复超时处理测试

**检查结果**: ✅ 测试覆盖完整，无需修复

1. **多轮会话连续执行测试**: ✅ 测试完整
   - 测试类: `TestMultiTurnSession`（7个测试用例）
   - 测试位置: `tests/test_task_api.py` 第 1126-1258 行
   - 测试内容:
     - `test_continue_conversation_gets_latest_session`: continue_conversation=True 时获取最近会话
     - `test_continue_conversation_no_session`: continue_conversation=True 但无会话时返回 None
     - `test_new_session_ignores_resume`: new_session=True 时忽略 resume 参数
     - `test_session_id_persistence_in_manager`: 会话 ID 在会话管理器中持久化
     - `test_multiple_sessions_independent`: 多个会话相互独立
     - `test_task_request_continue_conversation_default`: TaskRequest 的 continue_conversation 默认值为 False
     - `test_task_request_resume_none_by_default`: TaskRequest 的 resume 默认值为 None

2. **会话中断后恢复测试**: ✅ 测试完整
   - 测试类: `TestSessionRecoveryAfterInterruption`（8个测试用例）
   - 测试位置: `tests/test_task_api.py` 第 1264-1454 行
   - 测试内容:
     - `test_recovery_with_valid_session_file`: 有效会话文件的恢复
     - `test_recovery_session_file_with_assistant_message`: 带助手消息的会话文件恢复
     - `test_recovery_with_invalid_session_id`: 无效会话 ID 的恢复
     - `test_recovery_with_mismatched_session_id`: sessionId 不匹配时的恢复
     - `test_memory_session_and_file_session_independent`: 内存会话和文件会话相互独立（服务重启场景）
     - `test_get_latest_session_id_returns_none_for_empty_dir`: 空目录返回 None
     - `test_get_latest_session_id_handles_invalid_jsonl`: 包含无效 JSON 的会话目录

3. **超时处理测试**: ✅ 测试完整
   - 测试类: `TestTimeoutHandling`（14个测试用例）
   - 测试位置: `tests/test_task_api.py` 第 1460-1766 行
   - 测试内容:
     - `test_cleanup_old_sessions_max_age`: 会话总超时清理（3600秒）
     - `test_cleanup_old_sessions_activity_timeout`: 活动超时清理（1800秒）
     - `test_cleanup_answer_timeout_waiting_session`: 单次答案超时清理（300秒，等待状态的会话）
     - `test_cleanup_answer_timeout_not_waiting_session`: 非等待状态的会话不受答案超时影响
     - `test_cleanup_does_not_remove_recent_sessions`: 清理不会移除活跃会话
     - `test_cleanup_respects_max_age_priority`: 清理优先考虑会话总超时
     - `test_cleanup_with_zero_timeout`: 超时时间为0时的清理行为
     - `test_update_activity`: 更新会话活动时间
     - `test_update_activity_nonexistent_session`: 更新不存在会话的活动时间
     - `test_session_state_data_time_properties`: SessionStateData 时间属性
     - `test_session_info_time_fields`: SessionInfo 时间字段

4. **补充测试类（T3.7 已添加）**:
   - `TestStateTransitionCompleteness`（6个测试用例）: 状态转换完整性测试
   - `TestSessionRecoveryCompleteness`（6个测试用例）: 会话恢复流程完整性测试
   - `TestContextManagementCompleteness`（6个测试用例）: 上下文管理完整性测试

**测试统计**:
- 多轮会话相关测试用例总数: 47个
- 测试文件: `tests/test_task_api.py`（共 2275+ 行）
- 核心实现: `app/routers/session_manager.py`、`app/routers/task.py`

---

## T4 - 消息渲染与交互

**功能模块**: 消息渲染、问答交互、发送/停止/清空按钮

### T4.1 消息类型渲染需求检查与修复

**执行角色**: 产品经理

- [x] 检查并修复不同消息类型的渲染需求（文本/工具调用/错误）
- [x] 检查并修复消息时间戳显示需求
- [x] 检查并修复加载状态和进度显示需求

**检查结果**: ✅ 需求文档完整，无需修复

1. **不同消息类型的渲染需求（文本/工具调用/错误）**: 完整定义

   - **消息类型定义完整**: 7种消息类型（text/thinking/tool_use/tool_result/error/complete/ask_user_question）+ 1种规划中（info）
   - **每种类型的渲染规则明确**:
     - text: 普通文本，代码块使用等宽字体和语法高亮
     - thinking: 浅灰色背景，带思考图标，可折叠
     - tool_use: 绿色边框卡片，显示工具名称和参数
     - tool_result: 蓝色边框卡片，显示执行结果
     - error: 红色边框和文字，带错误图标
     - complete: 绿色成功提示，显示统计摘要
     - ask_user_question: 紫色边框卡片，交互式选项
   - **样式一致性要求完整**: 边框颜色、背景色、文字色均明确定义
   - **CSS 类名定义完整**: 每种类型都有对应的 CSS 类名规范
   - **定义位置**: `当前会话-消息类型.md` 第 1-2 节

2. **消息时间戳显示需求**: 完整定义

   - **时间戳显示规则完整**: 每种消息类型的时间戳显示行为已明确
     - text: 可选显示（实时流式消息默认不显示，避免视觉干扰）
     - thinking: 不显示
     - tool_use/tool_result/error/complete/ask_user_question: 必须显示
   - **时间戳格式**: 24小时制 `HH:mm:ss`（如 `14:30:05`）
   - **时间戳位置**: 消息卡片右侧，与消息内容垂直居中对齐
   - **特殊说明**: ask_user_question 的时间戳显示在问答对话框右上角
   - **定义位置**: `当前会话-消息类型.md` 第 2.3 节（第 140-158 行）

3. **加载状态和进度显示需求**: 完整定义

   - **流式消息加载指示器**: 正在连接/正在接收/消息计数三种状态
   - **执行按钮加载状态**: 默认/加载中/停止中三种状态
   - **问答对话框加载状态**: 等待答案/提交中/提交失败三种状态
   - **动画效果定义**: 执行按钮旋转图标、消息流动画、页面过渡动画
   - **定义位置**:
     - `当前会话-消息类型.md` 第 2.4 节（第 160-185 行）
     - `当前会话-前端界面设计.md` 第 4.2 节（加载动画）

**参考文档**:
- `docs/需求文档/当前会话功能/当前会话-消息类型.md` - 消息类型定义、样式规范、时间戳规则、加载状态
- `docs/需求文档/当前会话功能/当前会话-主需求.md` - 主需求文档（第 6.5 节 SSE 消息类型定义）
- `docs/需求文档/当前会话功能/当前会话-前端界面设计.md` - 前端界面设计规范

### T4.2 问答交互需求检查与修复

**执行角色**: 产品经理

- [x] 检查并修复问答对话框的交互流程
- [x] 检查并修复用户操作（发送/停止/清空）的反馈需求

**检查结果**: ⚠️ 基本完整，发现以下问题需修复

1. **问答对话框的交互流程**: ✅ 基本完整
   - **弹出流程**：完整定义
     - 触发条件：收到 `ask_user_question` 类型 SSE 消息
     - 触发时机：AI 需要用户确认/选择时
     - 前端响应：暂停流式输出，显示问答对话框
     - 定义位置：`当前会话-主需求.md` 第 4.1.4 节、6.5.2 节
   - **展示流程**：完整定义
     - 样式：紫色边框卡片，CSS 类名 `.message-ask-question`
     - 位置：输出显示区
     - 内容：header、question、options、确认/取消按钮
     - 时间戳：右上角显示（HH:mm:ss 格式）
     - 定义位置：`当前会话-消息类型.md` 第 2.3 节、5.3 节
   - **关闭流程**：完整定义
     - 用户提交答案后关闭
     - 超时后关闭（ANSWER_TIMEOUT 错误码）
     - 定义位置：`当前会话-主需求.md` 第 6.5.3 节
   - **问题发现**：
     - ❌ 取消按钮的具体行为未明确定义（发送什么内容？API 如何处理？）

2. **选项渲染、用户选择、答案提交的流程**: ✅ 完整定义
   - **选项渲染**：
     - 单选支持：multiSelect = false 时显示单选按钮
     - 多选支持：multiSelect = true 时显示复选框
     - 选项样式：`.ask-question-option` 类，选中样式 `.ask-question-option.selected`
     - 定义位置：`当前会话-消息类型.md` 第 5.3.1-5.3.2 节
   - **用户选择**：
     - 选择操作：点击选项高亮选中，可多选
     - 答案格式：单选为字符串，多选为字符串数组，文本输入为字符串
     - 定义位置：`当前会话-主需求.md` 第 4.3.1 节、`当前会话-消息类型.md` 第 5.4 节
   - **答案提交**：
     - 提交方式：点击"确认"按钮
     - API 端点：POST /api/task/answer
     - 请求参数：session_id、question_id、answer、resume
     - 响应格式：success、message
     - 提交流程图：完整定义
     - 定义位置：`当前会话-主需求.md` 第 6.6 节

3. **问答超时、取消的处理**: ⚠️ 部分定义
   - **问答超时处理**：完整定义
     - 超时时间：300 秒（5 分钟）
     - 超时处理：自动取消等待状态，发送 error 消息关闭 SSE 连接
     - 错误码：ANSWER_TIMEOUT
     - 状态转换：waiting → failed
     - 定义位置：`当前会话-会话状态.md` 第 5.1 节、`当前会话-主需求.md` 第 6.5.2.3 节
   - **取消处理**：⚠️ 需补充
     - 取消按钮：主需求提到"点击'取消'按钮"的操作
     - **问题**：取消按钮的具体行为（发送什么内容？API 如何处理？）未明确定义
     - 建议补充：定义取消按钮的 API 调用参数和后端处理逻辑

4. **多问答场景的处理**: ⚠️ 需补充
   - 单次答案超时定义：300 秒
   - 最大等待数：5
   - **问题发现**：
     - ❌ 多个问题同时出现时的处理方式未定义（如：是否排队显示？）
     - ❌ 用户回答一个问题后，另一个问题的处理方式未定义
     - ❌ 追问（follow_up_questions）的前端展示方式未定义（答案提交需求.md 第 5.3 节有验证规则，但无前端展示定义）

5. **发送按钮的反馈需求**: ✅ 完整定义
   - **点击后状态变化**：输入框禁用，按钮切换为"停止"
   - **加载状态**：
     - 默认：蓝色背景 #3b82f6，显示"发送"
     - 加载中：显示旋转图标，禁用状态，文字变为"执行中"
   - **禁用条件**：
     - prompt 为空时禁用
     - 任务执行中时禁用（显示停止按钮）
     - 等待用户答案时禁用
   - 定义位置：`当前会话-主需求.md` 第 4.2 节、`当前会话-前端界面设计.md` 第 2.3 节、`当前会话-消息类型.md` 第 2.4.2 节

6. **停止按钮的反馈需求**: ✅ 基本完整
   - **点击后状态变化**：
     - 发送 AbortController 请求
     - 任务中断，状态变化：running → failed
   - **停止后处理**：
     - 前端显示"任务已停止"提示
     - 发送 error 消息（error_code: USER_ABORT）
     - SSE 连接关闭
     - 保留已显示的消息
   - **样式状态**：
     - 停止按钮：红色背景 #ef4444
     - 停止中：显示"停止中"，禁用状态
   - 定义位置：`当前会话-主需求.md` 第 4.2 节、6.5.2.3 节、`当前会话-前端界面设计.md` 第 2.3 节
   - **问题发现**：
     - ⚠️ 停止按钮点击后是否有确认提示未明确定义（仅"新建会话"时有确认提示）

7. **清空按钮的反馈需求**: ⚠️ 需补充
   - **已定义内容**：
     - 触发方式：点击工具栏右侧的"清空"按钮
     - 响应：清空输出显示区，重置统计信息
     - 清空后：输出区显示占位提示
   - **问题发现**：
     - ❌ 清空按钮是否有确认提示未明确定义
     - ❌ 清空范围（是否清空会话ID、历史消息）未详细定义
     - ❌ 清空后会话状态如何变化未明确定义
   - 定义位置：`当前会话-主需求.md` 第 4.2 节

**修复优先级**:
- **P1（中）**：
  - 补充取消按钮的具体行为定义（API 调用参数、后端处理逻辑）
  - 补充清空按钮的确认提示、清空范围、清空后状态定义
- **P2（低）**：
  - 补充多问答场景的处理方式定义
  - 补充追问（follow_up_questions）的前端展示方式
  - 补充停止按钮的确认提示定义（可选）

**修复位置**:
- `docs/需求文档/当前会话功能/当前会话-主需求.md` - 补充取消按钮行为、清空按钮定义
- `docs/需求文档/当前会话功能/当前会话-消息类型.md` - 补充追问展示方式
- `docs/需求文档/当前会话功能/当前会话-答案提交-需求.md` - 补充取消 API 处理逻辑

**参考文档**:
- `docs/需求文档/当前会话功能/当前会话-主需求.md` - 主需求文档（第 4.1.4、4.2、4.3、6.5、6.6 节）
- `docs/需求文档/当前会话功能/当前会话-消息类型.md` - 消息类型定义（第 2.4、5 节）
- `docs/需求文档/当前会话功能/当前会话-前端界面设计.md` - 前端界面设计规范（第 2.3 节）
- `docs/需求文档/当前会话功能/当前会话-会话状态.md` - 会话状态管理（第 5、6 节）
- `docs/需求文档/当前会话功能/当前会话-答案提交-需求.md` - 答案提交 API 需求

### T4.3 消息组件设计检查与修复

**执行角色**: 架构师

- [x] 检查并修复前端消息组件设计
- [x] 检查并修复消息列表滚动和分页加载设计
- [x] 检查并修复工具调用结果的展示格式设计

**检查结果**: ✅ 符合设计规范，无需修复

1. **前端消息组件设计完整性**: ✅ 符合规范

   - **消息类型覆盖完整**: 代码实现了 7 种消息类型渲染，与需求文档 `当前会话-消息类型.md` 定义完全一致
     - `text`: 普通文本消息，蓝色边框 `.message-text`
     - `thinking`: 思考块，黄色背景，可折叠 `.message-thinking`
     - `tool_use`: 工具调用，绿色边框，可折叠 `.message-tool_use`
     - `tool_result`: 工具结果，蓝色边框，可折叠 `.message-tool_result`
     - `error`: 错误消息，红色边框+背景 `.message-error`
     - `complete`: 完成消息，绿色边框+背景，显示统计摘要 `.message-complete`
     - `ask_user_question`: 用户问答，紫色边框+背景 `.message-ask-question`
     - `info`: 提示消息（代码已实现 `.message-info`，与需求文档一致）

   - **样式一致性**: CSS 变量定义完整，与需求文档颜色规范一致
     - 文件位置: `web/static/css/variables.css` 第 20-31 行
     - 所有消息类型的边框色、背景色均使用 CSS 变量定义

   - **时间戳显示**: ✅ 符合需求文档定义
     - `text` 和 `thinking` 类型不显示时间戳（避免实时流式消息频繁更新）
     - `tool_use`、`tool_result`、`error`、`complete`、`ask_user_question` 类型显示时间戳
     - 时间戳格式: `HH:mm:ss`（使用 `Utils.formatTime()`）
     - 代码位置: `web/static/modules/task.js` 第 221 行、`web/static/modules/message-renderer/core.js` 第 345 行

   - **组件模块化设计**: ✅ 设计合理
     - `MessageRendererCore`: 核心渲染逻辑、轮次管理、折叠状态
     - `MessageRendererContent`: 内容块渲染（text/complete/info/ask_user_question）
     - `MessageRendererThinking`: 思考块渲染
     - `MessageRendererTools`: 工具调用和结果渲染
     - `MessageRendererToolResults`: 工具结果样式配置和类型渲染
     - 代码位置: `web/static/modules/message-renderer/` 目录

2. **消息列表滚动和分页加载设计**: ✅ 符合设计规范

   - **滚动到底部功能**: ✅ 完整实现
     - `Utils.scrollToBottom()`: 基础滚动功能
     - `ScrollToBottomButton` 组件: 滚动到底部按钮，支持阈值显示/隐藏、平滑滚动
     - 代码位置: `web/static/utils.js` 第 22-26 行、`web/static/components/scrollToBottom.js`

   - **虚拟列表支持**: ✅ 已实现
     - `VirtualList` 组件支持虚拟滚动，优化大列表性能
     - 支持懒加载（`onLoadMore` 回调）
     - 代码位置: `web/static/components/virtualList.js`

   - **消息列表渲染**: ✅ 支持轮次折叠
     - 按轮次分组显示（`_groupByRounds()`）
     - 支持折叠/展开状态持久化（localStorage）
     - 代码位置: `web/static/modules/message-renderer/core.js` 第 189-239 行

   - **历史消息加载**: ✅ 支持分页
     - `/api/sessions/{session_id}/messages` 接口支持 page/limit 参数
     - 前端支持加载更多（Questions 模块实现）

3. **工具调用结果展示格式设计**: ✅ 符合设计规范

   - **工具图标系统**: ✅ 完整实现
     - `ToolIcons.getToolIcon()`: 获取工具对应图标
     - `ToolIcons.getToolColorClass()`: 获取工具颜色类
     - 常用工具图标映射: Read(📖)、Write(📝)、Edit(✏️)、Bash(💻)、Glob(🔍)、Grep(🔎)、Task(🤖) 等
     - 代码位置: `web/static/modules/message-renderer/tools.js` 第 36-55 行

   - **工具预览系统**: ✅ 完整实现
     - `ToolPreview.getToolPreview()`: 生成工具预览文本
     - 代码位置: `web/static/modules/message-renderer/tools.js` 第 50-55 行

   - **工具结果样式配置**: ✅ 完整定义
     - 为每种工具定义独立的图标、CSS类、颜色、标签、最大长度
     - 支持: Read、Write、Edit、Bash、Glob、Grep、WebSearch、WebFetch、Task、TodoWrite
     - 代码位置: `web/static/modules/message-renderer/tool-results.js` 第 10-81 行

   - **可折叠设计**: ✅ 符合需求
     - 工具调用和结果默认可折叠
     - 特定工具（TodoWrite、Task）自动展开
     - 内容过长时自动截断
     - 代码位置: `web/static/modules/message-renderer/tools.js` 第 84-149 行

   - **专用渲染器支持**: ✅ 可扩展
     - 支持 `ToolRenderers` 注册专用输入/结果渲染器
     - `hasInputRenderer()` / `hasResultRenderer()` 检查渲染器可用性
     - 代码位置: `web/static/modules/message-renderer/tools.js` 第 98-127 行

**代码位置**:
- 消息渲染核心: `web/static/modules/message-renderer/core.js`
- 内容块渲染: `web/static/modules/message-renderer/content.js`
- 思考块渲染: `web/static/modules/message-renderer/thinking.js`
- 工具渲染: `web/static/modules/message-renderer/tools.js`
- 工具结果渲染: `web/static/modules/message-renderer/tool-results.js`
- CSS 变量: `web/static/css/variables.css`
- CSS 组件样式: `web/static/css/components.css`
- 滚动组件: `web/static/components/scrollToBottom.js`
- 虚拟列表: `web/static/components/virtualList.js`
- 任务模块: `web/static/modules/task.js`

**参考文档**:
- `docs/需求文档/当前会话功能/当前会话-消息类型.md` - 消息类型定义、样式规范
- `docs/需求文档/当前会话功能/当前会话-前端界面设计.md` - 前端界面设计规范

### T4.4 问答交互设计检查与修复

**执行角色**: 架构师

- [ ] 检查并修复问答交互的组件设计
- [ ] 检查并修复 SSE 消息前端处理流程设计

### T4.5 SSE 消息解析代码检查与修复

**执行角色**: 全栈工程师

- [ ] 检查并修复 SSE 消息解析逻辑
- [ ] 检查并修复工具调用消息的展示格式
- [ ] 检查并修复消息列表更新逻辑

### T4.6 交互状态代码检查与修复

**执行角色**: 全栈工程师

- [ ] 检查并修复问答对话框答案回传逻辑
- [ ] 检查并修复发送/停止/清空按钮的状态管理逻辑
- [ ] 检查并修复错误消息展示逻辑

### T4.7 消息解析测试与修复

**执行角色**: 技术经理

- [ ] 检查并修复消息格式化工具函数测试
- [ ] 检查并修复 SSE 消息解析测试
- [ ] 检查并修复消息类型判断逻辑测试

### T4.8 组件与交互测试与修复

**执行角色**: 技术经理

- [ ] 检查并修复消息渲染组件测试
- [ ] 检查并修复问答对话框组件测试
- [ ] 检查并修复按钮状态组件测试
- [ ] 检查并修复完整问答交互流程 E2E 测试
- [ ] 检查并修复发送/停止/清空功能 E2E 测试

---

## T5 - 安全与权限控制

**功能模块**: 权限控制、输入验证、安全规则、工作空间选择

### T5.1 工作空间隔离需求检查与修复

**执行角色**: 产品经理

- [ ] 检查并修复工作空间隔离的需求定义
- [ ] 检查并修复权限控制的安全要求
- [ ] 检查并修复日志记录的安全要求

### T5.2 输入验证需求检查与修复

**执行角色**: 产品经理

- [ ] 检查并修复用户输入的验证规则需求
- [ ] 检查并修复敏感操作的范围限定需求

### T5.3 路径与权限设计检查与修复

**执行角色**: 架构师

- [ ] 检查并修复路径验证机制的设计
- [ ] 检查并修复权限模式（allow/deny）的配置设计
- [ ] 检查并修复敏感操作的安全校验设计

### T5.4 请求安全设计检查与修复

**执行角色**: 架构师

- [ ] 检查并修复 API 请求大小限制设计
- [ ] 检查并修复 Rate Limiting 设计

### T5.5 路径安全代码检查与修复

**执行角色**: 全栈工程师

- [ ] 检查并修复路径遍历攻击防护逻辑
- [ ] 检查并修复 API 请求参数验证
- [ ] 检查并修复工作空间隔离实现

### T5.6 权限与敏感信息代码检查与修复

**执行角色**: 全栈工程师

- [ ] 检查并修复权限校验逻辑
- [ ] 检查并修复错误消息敏感信息泄露

### T5.7 安全逻辑测试与修复

**执行角色**: 技术经理

- [ ] 检查并修复路径验证函数测试
- [ ] 检查并修复输入参数验证测试
- [ ] 检查并修复权限校验逻辑测试

### T5.8 安全攻击测试与修复

**执行角色**: 技术经理

- [ ] 检查并修复路径遍历攻击防护安全测试
- [ ] 检查并修复请求伪造防护安全测试
- [ ] 检查并修复请求大小限制测试

---

## T6 - 集成验证与测试

**功能模块**: 完整功能验证、端到端测试、性能稳定性

### T6.1 功能联调验证与修复

**执行角色**: 技术经理

- [ ] 检查并修复任务提交到完成完整流程
- [ ] 检查并修复会话历史保存和加载
- [ ] 检查并修复多轮会话上下文连贯性
- [ ] 检查并修复异常情况下的错误处理和恢复
- [ ] 检查并修复前后端数据流完整性

### T6.2 E2E 测试与修复

**执行角色**: 技术经理

- [ ] 检查并修复 E2E：用户打开页面 → 创建会话 → 执行任务 → 查看结果
- [ ] 检查并修复 E2E：用户创建会话 → 执行多轮对话 → 查看历史
- [ ] 检查并修复 E2E：用户中断任务 → 恢复会话 → 继续执行
- [ ] 检查并修复 E2E：用户切换工作空间 → 验证数据隔离

### T6.3 性能稳定性测试与修复

**执行角色**: 技术经理

- [ ] 检查并修复长时间会话稳定性测试
- [ ] 检查并修复并发请求处理测试
- [ ] 检查并修复资源泄漏检查
- [ ] 检查并修复内存使用监控测试

---

**最后更新**: 2026-03-20 (T1.8, T2.1, T2.2, T2.3, T2.4, T2.5, T2.6, T2.7, T2.8, T3.1, T3.2, T3.3, T3.4, T3.5, T3.6, T3.7, T3.8, T4.1, T4.2, T4.3 已完成)
