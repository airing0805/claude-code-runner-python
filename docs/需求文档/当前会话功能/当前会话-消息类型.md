# 当前会话 - 消息类型

## 文档目的

本文档定义当前会话中所有消息类型的定义、展示规范和前端渲染规则。

---

## 目录

1. [消息类型定义](#1-消息类型定义)
2. [消息展示样式](#2-消息展示样式)
3. [tool_use 消息](#3-tool-use-消息)
4. [tool_result 消息](#4-tool-result-消息)
5. [ask_user_question 消息](#5-ask_user_question-消息)
6. [相关文档](#6-相关文档)

---

## 1. 消息类型定义

| 类型 | 说明 | 样式 | CSS 类名 |
|------|------|------|----------|
| text | 文本内容 | 蓝色边框 | `.message-text` |
| thinking | 思考过程 (v0.2.11+) | 黄色背景，可折叠 | `.message-thinking` |
| tool_use | 工具调用 | 绿色边框 | `.message-tool-use` |
| tool_result | 工具结果 | 蓝色边框 | `.message-tool-result` |
| error | 错误信息 | 红色边框，红色文字 | `.message-error` |
| complete | 任务完成 | 绿色边框，成功提示样式 | `.message-complete` |
| info | 提示信息 | 天蓝色边框 | `.message-info` |
| ask_user_question | 用户问答 (v0.x.x+) | 紫色边框，交互式选项 | `.message-ask-question` |

### 样式一致性要求

| 消息类型 | 边框颜色 | 背景色 | 文字色 | 示例 |
|----------|----------|--------|--------|
| text | `#3b82f6` (blue-500) | 透明 | 默认 | 蓝色边框卡片 |
| thinking | `#fbbf24` (amber-400) | `#fef3c7` (amber-100) | `#92400e` (amber-800) | 黄色背景可折叠 |
| tool_use | `#22c55e` (green-500) | 透明 | 默认 | 绿色边框卡片 |
| tool_result | `#3b82f6` (blue-500) | 透明 | 默认 | 蓝色边框卡片 |
| error | `#ef4444` (red-500) | `#fef2f2` (red-50) | `#dc2626` (red-600) | 红色边框+背景 |
| complete | `#22c55e` (green-500) | `#f0fdf4` (green-50) | `#15803d` (green-700) | 绿色边框+背景 |
| info | `#0ea5e9` (sky-500) | `#f0f9ff` (sky-50) | `#0369a1` (sky-700) | 天蓝色边框+背景 |
| ask_user_question | `#a855f7` (purple-500) | `#faf5ff` (purple-50) | `#7e22ce` (purple-700) | 紫色边框+背景 |

---

## 2. 消息展示样式

### 2.1 基础消息样式

```css
/* 消息容器基础样式 */
.message {
  border-radius: 8px;
  padding: 12px 16px;
  margin: 12px 0;
  border-left: 4px solid;
}

/* 文本消息 */
.message-text {
  border-left-color: #3b82f6;
  background-color: transparent;
}

/* 思考消息 */
.message-thinking {
  border-left-color: #fbbf24;
  background-color: #fef3c7;
  color: #92400e;
  border-radius: 6px;
  padding: 8px 12px;
  margin: 8px 0;
}

/* 工具调用 */
.message-tool-use {
  border-left-color: #22c55e;
  background-color: transparent;
}

/* 工具结果 */
.message-tool-result {
  border-left-color: #3b82f6;
  background-color: transparent;
}

/* 错误消息 */
.message-error {
  border-left-color: #ef4444;
  background-color: #fef2f2;
  color: #dc2626;
}

/* 完成消息 */
.message-complete {
  border-left-color: #22c55e;
  background-color: #f0fdf4;
  color: #15803d;
}

/* 提示消息 */
.message-info {
  border-left-color: #0ea5e9;
  background-color: #f0f9ff;
  color: #0369a1;
}

/* 用户问答 */
.message-ask-question {
  border-left-color: #a855f7;
  background-color: #faf5ff;
  color: #7e22ce;
  border-radius: 8px;
  padding: 16px;
  margin: 12px 0;
}
```

### 2.2 消息内容渲染

| 消息类型 | 内容渲染规则 |
|----------|------------|
| text | 普通文本，代码块使用等宽字体和语法高亮 |
| thinking | 浅灰色背景，带思考图标，可折叠 |
| tool_use | 绿色边框卡片，显示工具名称和参数 |
| tool_result | 蓝色边框卡片，显示执行结果 |
| error | 红色边框和文字，带错误图标 |
| complete | 绿色成功提示，显示统计摘要 |
| info | 天蓝色边框卡片，提示信息 |
| ask_user_question | 紫色边框卡片，交互式选项 |

---

## 3. tool_use 消息

### 3.1 消息结构

```python
@dataclass
class ToolUseMessage:
    type: MessageType           # MessageType.TOOL_USE
    content: str             # 工具调用描述文本
    timestamp: str           # ISO 8601 时间戳
    tool_name: str           # 工具名称，如 "Read", "Write"
    tool_input: dict         # 工具参数
    tool_use_id: str         # 工具调用 ID
```

### 3.2 JSON 格式

```json
{
  "type": "tool_use",
  "content": "调用工具: Read",
  "timestamp": "2026-03-04T10:00:00.000Z",
  "tool_name": "Read",
  "tool_input": {
    "file_path": "/path/to/file.py"
  },
  "tool_use_id": "call_abc123def456"
}
```

### 3.3 字段说明

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `type` | string | ✅ | 固定为 "tool_use" |
| `content` | string | ✅ | 工具调用描述文本，供前端展示 |
| `timestamp` | string | ✅ | ISO 8601 格式时间戳 |
| `tool_name` | string | ✅ | 工具名称，如 "Read", "Write", "Edit" 等 |
| `tool_input` | object | ✅ | 工具参数，结构由具体工具定义 |
| `tool_use_id` | string | ✅ | 唯一标识，用于与 tool_result 配对 |

### 3.4 常用工具名称

| 工具名 | 说明 | 图标 |
|-------|-----|------|
| Read | 读取文件 | 📖 |
| Write | 写入文件 | 📝 |
| Edit | 编辑文件 | 📝 |
| Bash | 执行命令 | 💻 |
| Glob | 文件模式匹配 | 🔍 |
| Grep | 内容搜索 | 🔎 |
| Task | 启动子代理 | 🤖 |

### 3.5 前端展示规则

| 工具 | 展示方式 |
|------|---------|
| Read | 显示文件路径 + 读取图标 | `📖 Read: src/main.py` |
| Write | 显示文件路径 + 写入图标 | `✏️ Write: src/utils.py` |
| Edit | 显示文件路径 + 编辑图标 | `📝 Edit: src/main.py` |
| Bash | 显示命令内容 + 执行图标 | `💻 Bash: pytest tests/` |
| Glob | 显示模式 + 搜索图标 | `🔍 Glob: *.py` |
| Grep | 显示模式 + 搜索图标 | `🔎 Grep: import` |

---

## 4. tool_result 消息

### 4.1 消息结构

```python
@dataclass
class ToolResultMessage:
    type: MessageType           # MessageType.TOOL_RESULT
    content: str             # 结果内容或错误信息
    timestamp: str           # ISO 8601 时间戳
    tool_use_id: str         # 关联的 tool_use ID
    is_error: bool           # 是否错误
    error_type: str | None   # 错误类型
    metadata: dict          # 附加元数据
```

### 4.2 JSON 格式

```json
{
  "type": "tool_result",
  "content": "文件内容或错误信息",
  "timestamp": "2026-03-04T10:00:00.000Z",
  "tool_use_id": "call_abc123def456",
  "is_error": false,
  "error_type": null,
  "metadata": {
    "exit_code": 0,
    "lines_read": 150
  }
}
```

### 4.3 字段说明

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `type` | string | ✅ | 固定为 "tool_result" |
| `content` | string | ✅ | 结果内容（成功时）或错误信息（失败时） |
| `timestamp` | string | ✅ | ISO 8601 格式时间戳 |
| `tool_use_id` | string | ✅ | 关联的 `tool_use.tool_use_id`，必须匹配 |
| `is_error` | boolean | ✅ | 是否执行失败 |
| `error_type` | string | ❌ | 错误类型（失败时） |
| `metadata` | object | ❌ | 附加元数据，根据工具类型而不同 |

### 4.4 与 tool_use 的关系

```
┌─────────────────────────────────────────────┐
│              工具调用完整流程                  │
└─────────────────────────────────────────────┘
                        │
                        ▼
[tool_use 消息]
  tool_name: "Read"
  tool_input: {"file_path": "src/main.py"}
  tool_use_id: "call_abc123"
                        │
                        ▼
                    工具执行
                        │
                        ▼
[tool_result 消息]
  tool_use_id: "call_abc123"  ← 配对 ID
  content: [文件内容]
  is_error: false
```

---

## 5. ask_user_question 消息

### 5.1 功能概述

当 Claude Code 在执行任务过程中需要用户确认、选择或输入信息时，会暂停执行并向用户提问。用户回答后，任务继续执行。

### 5.2 实现状态说明

| 功能需求 | SDK 支持状态 | 前端实现状态 | 说明 |
|---------|-------------|--------------|------|
| AskUserQuestion 工具调用 | ❌ 不支持 | ⏸ 预留接口 | 需等待 SDK 更新 |
| 问答对话框组件 | - | ❌ 未实现 | 待实现 |
| 交互式选项选择 | - | ❌ 未实现 | 待实现 |
| 答案提交 API | ✅ 支持 | ❌ 未实现 | 待实现 |
| 递归对话 | ❌ 不支持 | - | 需等待 SDK 更新 |
| 条件追问 | ❌ 不支持 | - | 需等待 SDK 更新 |

**当前可用替代方案：**
- 用户可以在任务描述中直接回答问题
- 使用 continue_conversation 或 resume 参数管理会话上下文

### 5.3 问答组件设计

#### 5.3.1 组件属性

| 属性 | 说明 |
|------|------|
| 标题 | 显示 header 字段内容 |
| 问题描述 | 显示 question 字段内容 |
| 选项列表 | 根据 options 数组渲染选项 |
| 多选支持 | multiSelect = true 时显示复选框 |
| 单选支持 | multiSelect = false 时显示单选按钮 |
| 确认按钮 | 提交用户选择 |
| 取消按钮 | 取消问答（可选） |

#### 5.3.2 组件样式

```css
/* 问答对话框样式 */
.ask-question-dialog {
  border: 2px solid #a855f7;
  background-color: #faf5ff;
  border-radius: 8px;
  padding: 16px;
  margin: 12px 0;
}

.ask-question-header {
  font-weight: bold;
  color: #7e22ce;
  margin-bottom: 8px;
}

.ask-question-text {
  color: #581c87;
  margin-bottom: 12px;
}

.ask-question-options {
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.ask-question-option {
  display: flex;
  align-items: center;
  padding: 8px 12px;
  border: 1px solid #d8b4fe;
  border-radius: 6px;
  cursor: pointer;
  transition: all 0.2s;
}

.ask-question-option:hover {
  background-color: #e9d5ff;
}

.ask-question-option.selected {
  background-color: #d8b4fe;
  border-color: #a855f7;
}

.ask-question-buttons {
  display: flex;
  justify-content: flex-end;
  gap: 8px;
  margin-top: 16px;
}
```

### 5.4 答案格式

**单选答案**:
```json
{
  "请选择一个功能": "背唐诗"
}
```

**多选答案**:
```json
{
  "请选择一个功能": ["背唐诗", "讲笑话"]
}
```

**文本输入答案**:
```json
{
  "自定义端口号": "8080"
}
```

---

## 6. 相关文档

| 文档 | 说明 |
|------|------|
| [当前会话-主需求.md](当前会话-主需求.md) | 主需求文档 |
| [当前会话-会话格式.md](当前会话-会话格式.md) | 会话文件格式定义 |
| [当前会话-会话状态.md](当前会话-会话状态.md) | 会话状态管理需求 |
| [当前会话-继续会话.md](当前会话-继续会话.md) | 继续会话详细需求 |
| [当前会话-答案提交.md](当前会话-答案提交.md) | 答案提交 API 详细需求 |
| [当前会话-多轮会话.md](当前会话-多轮会话.md) | 多轮会话业务场景需求 |
| [当前会话-概念辨析.md](当前会话-概念辨析.md) | 概念对比辨析 |

---

*文档版本：1.0*
*最后更新：2026-03-04*
