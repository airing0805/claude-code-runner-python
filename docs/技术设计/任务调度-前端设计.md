# 任务调度 v7.0.3 - 前端设计

**文档版本**: v1.0
**创建日期**: 2026-03-05
**执行角色**: 高级架构师 (senior-architect)
**任务编号**: v7.0.3

---

## 一、设计概述

### 1.1 技术栈

| 技术 | 用途 | 版本 |
|------|------|------|
| Vanilla JavaScript | 核心逻辑 | ES6+ |
| CSS3 | 样式设计 | - |
| Fetch API | 网络请求 | - |
| Server-Sent Events | 实时更新 | - |

### 1.2 模块结构

```
web/static/
├── modules/
│   ├── scheduler.js      # 调度器核心模块
│   ├── taskQueue.js      # 任务队列模块
│   └── scheduledTask.js  # 定时任务模块
├── components/
│   ├── taskDialog.js     # 任务创建对话框
│   ├── cronHelper.js     # Cron表达式助手
│   └── taskMonitor.js    # 任务监控组件
└── css/
    └── scheduler.css     # 调度器样式
```

---

## 二、页面布局设计

### 2.1 整体布局

```
┌────────────────────────────────────────────────────────────┐
│                        页面头部                             │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ 调度器状态: ● 运行中  │  队列: 5  运行: 1  定时: 10   │  │
│  │ [启动] [停止] [刷新]                                │  │
│  └──────────────────────────────────────────────────────┘  │
├────────────────────────────────────────────────────────────┤
│                      标签页导航                             │
│  [任务队列] [定时任务] [运行中] [已完成] [失败] [日志]    │
├────────────────────────────────────────────────────────────┤
│                                                            │
│                      标签页内容区                           │
│                                                            │
│  ┌──────────────────────────────────────────────────────┐  │
│  │                                                      │  │
│  │              根据选中的标签页显示不同内容              │  │
│  │                                                      │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                            │
└────────────────────────────────────────────────────────────┘
```

### 2.2 标签页功能

| 标签页 | 功能 | 操作 |
|--------|------|------|
| 任务队列 | 显示待执行任务 | 添加、删除、清空 |
| 定时任务 | 显示定时任务列表 | 创建、编辑、启用/禁用、立即执行、删除 |
| 运行中 | 显示正在执行的任务 | 查看、取消 |
| 已完成 | 显示成功任务（分页） | 查看详情 |
| 失败 | 显示失败任务（分页） | 查看详情、重试 |
| 日志 | 显示任务执行日志 | 搜索、清空 |

---

## 三、组件设计

### 3.1 调度器状态面板

```javascript
class SchedulerStatusPanel {
    constructor(container) {
        this.container = container;
        this.status = 'stopped';
        this.stats = {};
    }
    
    // 更新状态
    async updateStatus() {
        const response = await fetch('/api/scheduler/status');
        const data = await response.json();
        this.status = data.data.status;
        this.stats = data.data;
        this.render();
    }
    
    // 渲染UI
    render() {
        return `
            <div class="scheduler-status ${this.status}">
                <span class="status-indicator">
                    ${this.status === 'running' ? '● 运行中' : '○ 已停止'}
                </span>
                <span class="stats">
                    队列: ${this.stats.queue_count} | 
                    运行: ${this.stats.running_count} | 
                    定时: ${this.stats.scheduled_count}
                </span>
                <div class="actions">
                    <button onclick="startScheduler()">启动</button>
                    <button onclick="stopScheduler()">停止</button>
                    <button onclick="refreshData()">刷新</button>
                </div>
            </div>
        `;
    }
}
```

### 3.2 任务队列组件

```javascript
class TaskQueueComponent {
    constructor(container) {
        this.container = container;
        this.tasks = [];
    }
    
    // 获取任务列表
    async fetchTasks() {
        const response = await fetch('/api/scheduler/tasks');
        const data = await response.json();
        this.tasks = data.data;
        this.render();
    }
    
    // 删除任务
    async deleteTask(taskId) {
        if (confirm('确定要删除这个任务吗？')) {
            await fetch(`/api/scheduler/tasks/${taskId}`, { method: 'DELETE' });
            this.fetchTasks();
        }
    }
    
    // 清空队列
    async clearQueue() {
        if (confirm('确定要清空整个队列吗？')) {
            await fetch('/api/scheduler/tasks/clear', { method: 'DELETE' });
            this.fetchTasks();
        }
    }
    
    // 渲染任务列表
    render() {
        return `
            <div class="task-queue">
                <div class="toolbar">
                    <button onclick="openCreateTaskDialog()">添加任务</button>
                    <button onclick="clearQueue()">清空队列</button>
                </div>
                <table class="task-table">
                    <thead>
                        <tr>
                            <th>ID</th>
                            <th>任务描述</th>
                            <th>工作目录</th>
                            <th>超时</th>
                            <th>创建时间</th>
                            <th>操作</th>
                        </tr>
                    </thead>
                    <tbody>
                        ${this.tasks.map(task => `
                            <tr>
                                <td>${task.id.slice(0, 8)}...</td>
                                <td>${task.prompt.slice(0, 50)}...</td>
                                <td>${task.workspace}</td>
                                <td>${this.formatTimeout(task.timeout)}</td>
                                <td>${this.formatTime(task.created_at)}</td>
                                <td>
                                    <button onclick="viewTask('${task.id}')">查看</button>
                                    <button onclick="deleteTask('${task.id}')">删除</button>
                                </td>
                            </tr>
                        `).join('')}
                    </tbody>
                </table>
            </div>
        `;
    }
}
```

### 3.3 定时任务组件

```javascript
class ScheduledTaskComponent {
    constructor(container) {
        this.container = container;
        this.tasks = [];
    }
    
    // 切换启用状态
    async toggleTask(taskId) {
        await fetch(`/api/scheduler/scheduled-tasks/${taskId}/toggle`, {
            method: 'POST'
        });
        this.fetchTasks();
    }
    
    // 立即执行
    async runNow(taskId) {
        const response = await fetch(
            `/api/scheduler/scheduled-tasks/${taskId}/run`,
            { method: 'POST' }
        );
        const data = await response.json();
        alert(`任务已加入队列: ${data.data.task_id}`);
    }
    
    // 渲染定时任务列表
    render() {
        return `
            <div class="scheduled-tasks">
                <div class="toolbar">
                    <button onclick="openCreateScheduledDialog()">
                        创建定时任务
                    </button>
                </div>
                <table class="task-table">
                    <thead>
                        <tr>
                            <th>名称</th>
                            <th>Cron表达式</th>
                            <th>下次执行</th>
                            <th>执行次数</th>
                            <th>状态</th>
                            <th>操作</th>
                        </tr>
                    </thead>
                    <tbody>
                        ${this.tasks.map(task => `
                            <tr class="${task.enabled ? 'enabled' : 'disabled'}">
                                <td>${task.name}</td>
                                <td><code>${task.cron}</code></td>
                                <td>${task.next_run ? this.formatTime(task.next_run) : '-'}</td>
                                <td>
                                    ${task.run_count} 
                                    (成功: ${task.success_count}, 失败: ${task.failed_count})
                                </td>
                                <td>
                                    <span class="badge ${task.enabled ? 'enabled' : 'disabled'}">
                                        ${task.enabled ? '启用' : '禁用'}
                                    </span>
                                </td>
                                <td>
                                    <button onclick="editScheduled('${task.id}')">编辑</button>
                                    <button onclick="toggleScheduled('${task.id}')">
                                        ${task.enabled ? '禁用' : '启用'}
                                    </button>
                                    <button onclick="runScheduledNow('${task.id}')">立即执行</button>
                                    <button onclick="deleteScheduled('${task.id}')">删除</button>
                                </td>
                            </tr>
                        `).join('')}
                    </tbody>
                </table>
            </div>
        `;
    }
}
```

### 3.4 任务创建对话框

```javascript
class CreateTaskDialog {
    constructor() {
        this.isOpen = false;
    }
    
    open() {
        this.isOpen = true;
        this.render();
    }
    
    close() {
        this.isOpen = false;
        document.getElementById('dialog-overlay').remove();
    }
    
    async submit(formData) {
        const response = await fetch('/api/scheduler/tasks', {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify(formData)
        });
        
        if (response.ok) {
            this.close();
            window.taskQueue.fetchTasks();
        } else {
            const error = await response.json();
            this.showError(error.detail.error);
        }
    }
    
    render() {
        return `
            <div id="dialog-overlay" class="dialog-overlay">
                <div class="dialog">
                    <h3>创建任务</h3>
                    <form onsubmit="submitTask(event)">
                        <div class="form-group">
                            <label>任务描述 *</label>
                            <textarea name="prompt" required 
                                placeholder="输入任务描述..."
                                maxlength="10000"></textarea>
                        </div>
                        
                        <div class="form-group">
                            <label>工作目录</label>
                            <input type="text" name="workspace" 
                                placeholder="默认为当前工作目录">
                        </div>
                        
                        <div class="form-group">
                            <label>超时时间（毫秒）</label>
                            <input type="number" name="timeout" 
                                min="1000" max="3600000" 
                                value="600000">
                            <span class="hint">范围: 1000-3600000 (1秒-1小时)</span>
                        </div>
                        
                        <div class="form-group">
                            <label>工具限制</label>
                            <select name="allowed_tools" multiple>
                                <option value="Read">Read</option>
                                <option value="Write">Write</option>
                                <option value="Edit">Edit</option>
                                <option value="Glob">Glob</option>
                                <option value="Grep">Grep</option>
                                <option value="Bash">Bash</option>
                            </select>
                            <span class="hint">不选择表示不限制</span>
                        </div>
                        
                        <div class="form-group">
                            <label>
                                <input type="checkbox" name="auto_approve">
                                自动批准工具操作
                            </label>
                            <span class="warning">⚠️ 启用后所有工具操作将自动执行</span>
                        </div>
                        
                        <div class="actions">
                            <button type="button" onclick="closeDialog()">取消</button>
                            <button type="submit">创建任务</button>
                        </div>
                    </form>
                </div>
            </div>
        `;
    }
}
```

### 3.5 Cron表达式助手

```javascript
class CronHelper {
    constructor() {
        this.examples = [
            { expression: '*/5 * * * *', description: '每 5 分钟' },
            { expression: '0 * * * *', description: '每小时' },
            { expression: '0 */2 * * *', description: '每 2 小时' },
            { expression: '0 9 * * *', description: '每天 9:00' },
            { expression: '0 9 * * 1-5', description: '工作日 9:00' },
            { expression: '0 0 * * *', description: '每天零点' },
        ];
    }
    
    async validate(cron) {
        const response = await fetch('/api/scheduler/validate-cron', {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({ cron })
        });
        
        if (response.ok) {
            const data = await response.json();
            return {
                valid: true,
                nextRuns: data.data.next_runs
            };
        } else {
            return { valid: false };
        }
    }
    
    render() {
        return `
            <div class="cron-helper">
                <div class="quick-select">
                    <label>快速选择</label>
                    <select onchange="selectCronPreset(this.value)">
                        <option value="">自定义表达式</option>
                        ${this.examples.map(ex => `
                            <option value="${ex.expression}">
                                ${ex.description} (${ex.expression})
                            </option>
                        `).join('')}
                    </select>
                </div>
                
                <div class="preview">
                    <label>下次执行时间</label>
                    <ul id="next-runs">
                        <li>输入表达式后显示</li>
                    </ul>
                </div>
            </div>
        `;
    }
}
```

---

## 四、样式设计

### 4.1 色彩系统

```css
:root {
    /* 主色调 */
    --primary-color: #4a90d9;
    --primary-hover: #357abd;
    
    /* 状态色 */
    --success-color: #28a745;
    --warning-color: #ffc107;
    --danger-color: #dc3545;
    --info-color: #17a2b8;
    
    /* 中性色 */
    --gray-100: #f8f9fa;
    --gray-200: #e9ecef;
    --gray-300: #dee2e6;
    --gray-600: #6c757d;
    --gray-900: #212529;
    
    /* 背景色 */
    --bg-color: #ffffff;
    --bg-secondary: #f5f5f5;
}
```

### 4.2 任务状态样式

```css
/* 状态徽章 */
.badge {
    padding: 2px 8px;
    border-radius: 4px;
    font-size: 12px;
    font-weight: 500;
}

.badge.pending { background: var(--warning-color); color: #000; }
.badge.running { background: var(--info-color); color: #fff; }
.badge.completed { background: var(--success-color); color: #fff; }
.badge.failed { background: var(--danger-color); color: #fff; }
.badge.cancelled { background: var(--gray-600); color: #fff; }
.badge.enabled { background: var(--success-color); color: #fff; }
.badge.disabled { background: var(--gray-300); color: var(--gray-900); }

/* 调度器状态 */
.scheduler-status.running {
    background: linear-gradient(135deg, #e8f5e9, #c8e6c9);
}

.scheduler-status.stopped {
    background: linear-gradient(135deg, #ffebee, #ffcdd2);
}

.status-indicator {
    font-weight: bold;
}

.status-indicator.running { color: var(--success-color); }
.status-indicator.stopped { color: var(--danger-color); }
```

### 4.3 表格样式

```css
.task-table {
    width: 100%;
    border-collapse: collapse;
}

.task-table th,
.task-table td {
    padding: 12px;
    text-align: left;
    border-bottom: 1px solid var(--gray-300);
}

.task-table th {
    background: var(--bg-secondary);
    font-weight: 600;
}

.task-table tr:hover {
    background: var(--gray-100);
}

.task-table tr.disabled {
    opacity: 0.6;
}
```

### 4.4 对话框样式

```css
.dialog-overlay {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: rgba(0, 0, 0, 0.5);
    display: flex;
    align-items: center;
    justify-content: center;
    z-index: 1000;
}

.dialog {
    background: var(--bg-color);
    border-radius: 8px;
    padding: 24px;
    max-width: 600px;
    width: 90%;
    max-height: 80vh;
    overflow-y: auto;
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.15);
}

.dialog h3 {
    margin: 0 0 16px;
    font-size: 18px;
}

.form-group {
    margin-bottom: 16px;
}

.form-group label {
    display: block;
    margin-bottom: 4px;
    font-weight: 500;
}

.form-group input,
.form-group textarea,
.form-group select {
    width: 100%;
    padding: 8px;
    border: 1px solid var(--gray-300);
    border-radius: 4px;
}

.form-group textarea {
    min-height: 100px;
    resize: vertical;
}

.hint {
    display: block;
    font-size: 12px;
    color: var(--gray-600);
    margin-top: 4px;
}

.warning {
    color: var(--warning-color);
    font-size: 12px;
}
```

---

## 五、交互设计

### 5.1 自动刷新机制

```javascript
class AutoRefresher {
    constructor(interval = 5000) {
        this.interval = interval;
        this.timer = null;
        this.enabled = true;
    }
    
    start(callback) {
        this.timer = setInterval(() => {
            if (this.enabled) {
                callback();
            }
        }, this.interval);
    }
    
    stop() {
        if (this.timer) {
            clearInterval(this.timer);
            this.timer = null;
        }
    }
    
    pause() {
        this.enabled = false;
    }
    
    resume() {
        this.enabled = true;
    }
}

// 使用
const refresher = new AutoRefresher(5000);
refresher.start(() => {
    updateSchedulerStatus();
    refreshCurrentTab();
});
```

### 5.2 实时状态更新

```javascript
class StatusUpdater {
    constructor() {
        this.eventSource = null;
    }
    
    connect() {
        // 使用 SSE 实现实时更新（可选）
        this.eventSource = new EventSource('/api/scheduler/events');
        
        this.eventSource.onmessage = (event) => {
            const data = JSON.parse(event.data);
            this.handleUpdate(data);
        };
    }
    
    handleUpdate(data) {
        switch (data.type) {
            case 'task_started':
                this.onTaskStarted(data.task);
                break;
            case 'task_completed':
                this.onTaskCompleted(data.task);
                break;
            case 'task_failed':
                this.onTaskFailed(data.task);
                break;
            case 'scheduler_status':
                this.onSchedulerStatusChange(data.status);
                break;
        }
    }
    
    disconnect() {
        if (this.eventSource) {
            this.eventSource.close();
        }
    }
}
```

### 5.3 确认对话框

```javascript
class ConfirmDialog {
    static show(message, onConfirm, onCancel) {
        const overlay = document.createElement('div');
        overlay.className = 'dialog-overlay';
        overlay.innerHTML = `
            <div class="dialog confirm-dialog">
                <p>${message}</p>
                <div class="actions">
                    <button class="btn-secondary" id="cancel-btn">取消</button>
                    <button class="btn-danger" id="confirm-btn">确定</button>
                </div>
            </div>
        `;
        
        document.body.appendChild(overlay);
        
        document.getElementById('confirm-btn').onclick = () => {
            overlay.remove();
            onConfirm();
        };
        
        document.getElementById('cancel-btn').onclick = () => {
            overlay.remove();
            if (onCancel) onCancel();
        };
    }
}

// 使用
ConfirmDialog.show(
    '确定要删除这个任务吗？',
    () => deleteTask(taskId)
);
```

---

## 六、错误处理

### 6.1 错误提示组件

```javascript
class Toast {
    static show(message, type = 'info', duration = 3000) {
        const toast = document.createElement('div');
        toast.className = `toast toast-${type}`;
        toast.textContent = message;
        
        document.getElementById('toast-container').appendChild(toast);
        
        setTimeout(() => {
            toast.classList.add('fade-out');
            setTimeout(() => toast.remove(), 300);
        }, duration);
    }
    
    static success(message) {
        this.show(message, 'success');
    }
    
    static error(message) {
        this.show(message, 'error');
    }
    
    static warning(message) {
        this.show(message, 'warning');
    }
}

// 样式
const toastStyles = `
.toast-container {
    position: fixed;
    top: 20px;
    right: 20px;
    z-index: 2000;
}

.toast {
    padding: 12px 24px;
    margin-bottom: 8px;
    border-radius: 4px;
    color: #fff;
    animation: slideIn 0.3s ease;
}

.toast-success { background: var(--success-color); }
.toast-error { background: var(--danger-color); }
.toast-warning { background: var(--warning-color); color: #000; }
.toast-info { background: var(--info-color); }

.toast.fade-out {
    animation: slideOut 0.3s ease;
}
`;
```

### 6.2 API 错误处理

```javascript
async function apiCall(url, options = {}) {
    try {
        const response = await fetch(url, {
            ...options,
            headers: {
                'Content-Type': 'application/json',
                ...options.headers
            }
        });
        
        const data = await response.json();
        
        if (!response.ok) {
            throw new APIError(
                data.detail?.error || '请求失败',
                data.detail?.code || 'UNKNOWN_ERROR',
                response.status
            );
        }
        
        return data;
        
    } catch (error) {
        if (error instanceof APIError) {
            Toast.error(error.message);
            throw error;
        }
        
        // 网络错误
        Toast.error('网络请求失败');
        throw new Error('Network Error');
    }
}

class APIError extends Error {
    constructor(message, code, status) {
        super(message);
        this.code = code;
        this.status = status;
    }
}
```

---

## 七、性能优化

### 7.1 虚拟滚动（大量数据时）

```javascript
class VirtualList {
    constructor(container, itemHeight = 60) {
        this.container = container;
        this.itemHeight = itemHeight;
        this.items = [];
        this.visibleStart = 0;
        this.visibleEnd = 0;
    }
    
    setItems(items) {
        this.items = items;
        this.updateVisibleItems();
    }
    
    updateVisibleItems() {
        const scrollTop = this.container.scrollTop;
        const containerHeight = this.container.clientHeight;
        
        this.visibleStart = Math.floor(scrollTop / this.itemHeight);
        this.visibleEnd = Math.ceil((scrollTop + containerHeight) / this.itemHeight);
        
        this.render();
    }
    
    render() {
        const visibleItems = this.items.slice(
            Math.max(0, this.visibleStart - 5),
            Math.min(this.items.length, this.visibleEnd + 5)
        );
        
        // 只渲染可见项
        this.container.innerHTML = visibleItems.map((item, index) => 
            this.renderItem(item, this.visibleStart + index)
        ).join('');
    }
}
```

### 7.2 防抖和节流

```javascript
// 防抖
function debounce(func, wait) {
    let timeout;
    return function executedFunction(...args) {
        const later = () => {
            clearTimeout(timeout);
            func(...args);
        };
        clearTimeout(timeout);
        timeout = setTimeout(later, wait);
    };
}

// 节流
function throttle(func, limit) {
    let inThrottle;
    return function(...args) {
        if (!inThrottle) {
            func.apply(this, args);
            inThrottle = true;
            setTimeout(() => inThrottle = false, limit);
        }
    };
}

// 使用示例
const debouncedSearch = debounce((query) => {
    searchTasks(query);
}, 300);
```

---

## 八、可访问性

### 8.1 键盘导航

```javascript
// Tab 键导航
document.addEventListener('keydown', (e) => {
    if (e.key === 'Tab') {
        document.body.classList.add('keyboard-nav');
    }
});

document.addEventListener('mousedown', () => {
    document.body.classList.remove('keyboard-nav');
});

// 快捷键
document.addEventListener('keydown', (e) => {
    // Ctrl/Cmd + N: 新建任务
    if ((e.ctrlKey || e.metaKey) && e.key === 'n') {
        e.preventDefault();
        openCreateTaskDialog();
    }
    
    // Escape: 关闭对话框
    if (e.key === 'Escape') {
        closeAllDialogs();
    }
});
```

### 8.2 ARIA 标签

```html
<!-- 按钮示例 -->
<button 
    aria-label="创建新任务"
    aria-describedby="create-task-hint"
    onclick="openCreateTaskDialog()">
    添加任务
</button>
<span id="create-task-hint" class="sr-only">
    点击创建一个新的任务到队列
</span>

<!-- 状态示例 -->
<div 
    role="status"
    aria-live="polite"
    aria-label="调度器状态">
    <span class="status-indicator running">运行中</span>
</div>

<!-- 表格示例 -->
<table role="grid" aria-label="任务队列">
    <thead>
        <tr>
            <th scope="col">任务ID</th>
            <th scope="col">描述</th>
            <th scope="col">操作</th>
        </tr>
    </thead>
    <tbody>
        <!-- ... -->
    </tbody>
</table>
```

---

## 九、测试策略

### 9.1 单元测试

| 组件 | 测试重点 |
|------|---------|
| CronHelper | Cron表达式验证 |
| TaskQueue | CRUD操作 |
| ScheduledTask | 启用/禁用逻辑 |
| ConfirmDialog | 确认/取消回调 |

### 9.2 E2E测试场景

1. 创建任务 → 查看队列 → 删除任务
2. 创建定时任务 → 启用/禁用 → 立即执行
3. 启动调度器 → 查看状态 → 停止调度器
4. 查看已完成任务 → 分页导航
5. 错误处理：无效输入、网络错误

---

## 十、附录

### A. 事件流图

```
用户操作 → UI组件 → API调用 → 服务器处理 → 返回结果 → 更新UI
    │                                                    │
    │                                                    │
    └──────────────── Toast提示 ◄────────────────────────┘
```

### B. 组件依赖关系

```
App
├── SchedulerStatusPanel
├── TabNavigator
│   ├── TaskQueueTab
│   │   ├── TaskTable
│   │   └── CreateTaskDialog
│   ├── ScheduledTaskTab
│   │   ├── ScheduledTable
│   │   ├── CreateScheduledDialog
│   │   └── CronHelper
│   ├── RunningTaskTab
│   ├── CompletedTaskTab
│   ├── FailedTaskTab
│   └── LogsTab
└── Toast
```

---

**文档结束**

*本文档由高级架构师 (senior-architect) 生成，用于指导前端开发工作。*
