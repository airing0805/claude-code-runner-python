# 项目文件浏览器 - 技术设计

**文档版本**: v1.0
**创建日期**: 2026-03-10
**执行角色**: 高级架构师 (senior-architect)

---

## 一、系统架构设计

### 1.1 三层架构

```
┌─────────────────────────────────────────────────────────────┐
│                      表现层 (API Layer)                     │
│  FastAPI 路由: app/routers/files.py                         │
│  提供 REST API 端点                                        │
└─────────────────────────┬───────────────────────────────────┘
                          │
┌─────────────────────────▼───────────────────────────────────┐
│                    业务逻辑层 (Service Layer)                │
│  文件服务: app/services/file_service.py                     │
│  负责目录遍历、文件读取、搜索等业务逻辑                     │
└─────────────────────────┬───────────────────────────────────┘
                          │
┌─────────────────────────▼───────────────────────────────────┐
│                    数据访问层 (Data Layer)                   │
│  文件系统操作: Python pathlib, os                           │
│  无需持久化存储                                            │
└─────────────────────────────────────────────────────────────┘
```

### 1.2 核心组件

| 组件 | 职责 | 文件 |
|------|------|------|
| FilesRouter | API 路由入口，请求验证 | routers/files.py |
| FileService | 业务逻辑处理，路径安全检查 | services/file_service.py |
| FileValidator | 路径验证，安全检查 | services/file_service.py |
| TreeBuilder | 目录树构建 | services/file_service.py |
| FileReader | 文件内容读取 | services/file_service.py |

---

## 二、数据模型设计

### 2.1 目录树节点 (FileTreeNode)

```python
from dataclasses import dataclass, field
from typing import Optional

@dataclass
class FileTreeNode:
    """目录树节点"""
    name: str                          # 文件/目录名称
    path: str                         # 完整路径
    type: Literal["file", "directory"] # 类型
    size: Optional[int] = None        # 文件大小（字节）
    extension: Optional[str] = None   # 文件扩展名
    modified: Optional[str] = None    # 修改时间 (ISO 8601)
    children: list["FileTreeNode"] = field(default_factory=list)  # 子节点
    metadata: dict = field(default_factory=dict)  # 额外元数据
```

### 2.2 文件内容 (FileContent)

```python
@dataclass
class FileContent:
    """文件内容"""
    path: str                         # 文件路径
    name: str                         # 文件名
    size: int                         # 文件大小
    total_lines: int                  # 总行数
    content: str                      # 文件内容
    truncated: bool                   # 是否被截断
    has_more: bool                   # 是否还有更多内容
    encoding: str = "utf-8"          # 文件编码
```

### 2.3 文件信息 (FileInfo)

```python
@dataclass
class FileInfo:
    """文件信息"""
    path: str                         # 文件路径
    name: str                         # 文件名
    type: Literal["file", "directory"] # 类型
    size: int                         # 文件大小
    size_formatted: str              # 格式化大小 (如 "2.3 KB")
    extension: Optional[str] = None   # 扩展名
    mime_type: Optional[str] = None  # MIME 类型
    modified: Optional[str] = None    # 修改时间
    created: Optional[str] = None     # 创建时间
```

### 2.4 搜索结果 (SearchResult)

```python
@dataclass
class SearchResult:
    """搜索结果"""
    pattern: str                      # 搜索模式
    matches: list[dict]               # 匹配文件列表
    total: int                        # 总数
    truncated: bool                   # 是否被截断
```

---

## 三、API 接口详细设计

### 3.1 统一响应格式

```python
class APIResponse(BaseModel, Generic[T]):
    """统一 API 响应"""
    success: bool
    data: Optional[T] = None
    error: Optional[str] = None

class ErrorResponse(BaseModel):
    """错误响应"""
    success: bool = False
    error: str
    code: str  # 错误代码
```

### 3.2 API 端点详细定义

#### 3.2.1 获取目录文件树

```
GET /api/files/tree

Query Parameters:
| 参数 | 类型 | 必填 | 默认值 | 说明 |
|------|------|------|--------|------|
| path | string | 否 | 工作目录 | 目录路径 |
| depth | int | 否 | 2 | 递归深度 (1-5) |
| include_hidden | bool | 否 | false | 是否包含隐藏文件 |

Response 200:
{
  "success": true,
  "data": {
    "path": "/home/user/project",
    "name": "project",
    "type": "directory",
    "children": [
      {
        "name": "src",
        "type": "directory",
        "children": [
          {"name": "main.py", "type": "file", "size": 2345, "extension": ".py"}
        ]
      }
    ],
    "metadata": {"total_files": 25, "total_dirs": 8}
  }
}

Response 400:
{"success": false, "error": "无效的路径", "code": "INVALID_PATH"}

Response 403:
{"success": false, "error": "路径超出允许范围", "code": "FORBIDDEN_PATH"}

Response 404:
{"success": false, "error": "目录不存在", "code": "NOT_FOUND"}
```

#### 3.2.2 读取文件内容

```
GET /api/files/read

Query Parameters:
| 参数 | 类型 | 必填 | 默认值 | 说明 |
|------|------|------|--------|------|
| path | string | 是 | - | 文件路径 |
| start_line | int | 否 | 1 | 起始行号 |
| limit | int | 否 | 500 | 读取行数 (1-1000) |

Response 200:
{
  "success": true,
  "data": {
    "path": "/home/user/project/src/main.py",
    "name": "main.py",
    "size": 2345,
    "total_lines": 89,
    "content": "def main():\n    ...",
    "truncated": false,
    "has_more": false
  }
}

Response 400:
{"success": false, "error": "参数验证失败", "code": "VALIDATION_ERROR"}

Response 403:
{"success": false, "error": "禁止访问此文件", "code": "FORBIDDEN_FILE"}

Response 404:
{"success": false, "error": "文件不存在", "code": "FILE_NOT_FOUND"}

Response 413:
{"success": false, "error": "文件过大", "code": "FILE_TOO_LARGE"}
```

#### 3.2.3 搜索文件

```
GET /api/files/search

Query Parameters:
| 参数 | 类型 | 必填 | 默认值 | 说明 |
|------|------|------|--------|------|
| pattern | string | 是 | - | Glob 模式 (如 **/*.py) |
| path | string | 否 | 工作目录 | 搜索目录 |
| limit | int | 否 | 100 | 限制结果数量 |

Response 200:
{
  "success": true,
  "data": {
    "pattern": "**/*.py",
    "matches": [
      {"path": "/project/src/main.py", "name": "main.py", "size": 2345},
      {"path": "/project/src/utils.py", "name": "utils.py", "size": 1024}
    ],
    "total": 25,
    "truncated": false
  }
}
```

#### 3.2.4 获取文件信息

```
GET /api/files/info

Query Parameters:
| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| path | string | 是 | 文件路径 |

Response 200:
{
  "success": true,
  "data": {
    "path": "/home/user/project/src/main.py",
    "name": "main.py",
    "type": "file",
    "size": 2345,
    "size_formatted": "2.3 KB",
    "extension": ".py",
    "mime_type": "text/x-python",
    "modified": "2026-02-19T10:30:00Z"
  }
}
```

#### 3.2.5 Glob 模式查询

```
POST /api/files/glob

Request Body:
{
  "pattern": "src/**/*.py",  // 必填
  "path": "/project",        // 可选，默认工作目录
  "limit": 200               // 可选，默认100
}

Response 200:
{
  "success": true,
  "data": {
    "pattern": "src/**/*.py",
    "matches": ["src/main.py", "src/utils.py", "src/models/user.py"],
    "count": 3
  }
}
```

### 3.3 错误代码

| 代码 | HTTP 状态码 | 描述 |
|------|-------------|------|
| INVALID_PATH | 400 | 无效的路径格式 |
| FORBIDDEN_PATH | 403 | 路径超出允许范围 |
| NOT_FOUND | 404 | 目录/文件不存在 |
| FILE_IS_DIRECTORY | 400 | 路径指向目录 |
| FORBIDDEN_FILE | 403 | 禁止访问此文件类型 |
| FILE_TOO_LARGE | 413 | 文件超过大小限制 |
| VALIDATION_ERROR | 400 | 参数验证失败 |
| READ_ERROR | 500 | 文件读取错误 |

---

## 四、安全控制设计

### 4.1 路径验证器

```python
class PathValidator:
    """路径安全验证器"""

    # 敏感文件/目录模式
    SENSITIVE_PATTERNS = [
        r"\.env$",
        r"\.pem$",
        r"\.key$",
        r"\.secret$",
        r"\.password$",
        r"\.git/config$",
    ]

    # 禁止访问的系统目录
    FORBIDDEN_DIRS = {
        # Unix/Linux
        "/etc", "/root", "/boot", "/dev", "/proc", "/sys",
        "/var/log", "/var/lib",
        # Windows
        r"c:\windows", r"c:\system32", r"c:\program files",
    }

    def validate(self, path: str, base_dir: Path) -> Path:
        """验证路径安全性"""
        # 1. 解析绝对路径
        abs_path = (base_dir / path).resolve()

        # 2. 检查是否在基础目录内
        try:
            abs_path.relative_to(base_dir.resolve())
        except ValueError:
            raise PathValidationError("路径超出允许范围", "FORBIDDEN_PATH")

        # 3. 检查路径遍历攻击
        if ".." in Path(path).parts:
            raise PathValidationError("不允许路径遍历", "INVALID_PATH")

        # 4. 检查敏感文件
        for pattern in self.SENSITIVE_PATTERNS:
            if re.search(pattern, str(abs_path), re.IGNORECASE):
                raise PathValidationError("禁止访问敏感文件", "FORBIDDEN_FILE")

        # 5. 检查系统目录
        path_str = str(abs_path).lower()
        for forbidden in self.FORBIDDEN_DIRS:
            if path_str.startswith(forbidden.lower()):
                raise PathValidationError("禁止访问系统目录", "FORBIDDEN_PATH")

        return abs_path
```

### 4.2 文件大小限制

```python
# 配置常量
MAX_FILE_SIZE = 10 * 1024 * 1024      # 10 MB
MAX_READ_LINES = 1000                 # 最多读取行数
MAX_TREE_DEPTH = 5                    # 目录树最大深度
MAX_SEARCH_RESULTS = 500               # 搜索结果最大数量
```

### 4.3 编码处理

```python
# 支持的编码，按优先级
SUPPORTED_ENCODINGS = ["utf-8", "gbk", "gb2312", "latin-1"]

def detect_encoding(file_path: Path) -> str:
    """检测文件编码"""
    for encoding in SUPPORTED_ENCODINGS:
        try:
            file_path.read_text(encoding=encoding)
            return encoding
        except UnicodeDecodeError:
            continue
    return "utf-8"  # 默认 UTF-8，忽略错误
```

---

## 五、目录遍历算法设计

### 5.1 目录树构建

```python
class TreeBuilder:
    """目录树构建器"""

    def build(
        self,
        root_path: Path,
        depth: int = 2,
        include_hidden: bool = False,
    ) -> FileTreeNode:
        """构建目录树"""
        if not root_path.exists():
            raise FileNotFoundError(f"目录不存在: {root_path}")

        if not root_path.is_dir():
            raise NotADirectoryError(f"不是目录: {root_path}")

        node = FileTreeNode(
            name=root_path.name,
            path=str(root_path),
            type="directory",
        )

        if depth > 0:
            node.children = self._build_children(
                root_path,
                depth - 1,
                include_hidden,
            )

        # 统计
        node.metadata["total_files"] = self._count_files(node)
        node.metadata["total_dirs"] = self._count_dirs(node)

        return node

    def _build_children(
        self,
        parent: Path,
        depth: int,
        include_hidden: bool,
    ) -> list[FileTreeNode]:
        """递归构建子节点"""
        children = []

        try:
            entries = sorted(
                parent.iterdir(),
                key=lambda e: (not e.is_dir(), e.name.lower()),
            )
        except PermissionError:
            return []

        for entry in entries:
            # 跳过隐藏文件
            if not include_hidden and entry.name.startswith("."):
                continue

            if entry.is_dir():
                node = FileTreeNode(
                    name=entry.name,
                    path=str(entry),
                    type="directory",
                )
                if depth > 0:
                    node.children = self._build_children(
                        entry,
                        depth - 1,
                        include_hidden,
                    )
                children.append(node)
            elif entry.is_file():
                stat = entry.stat()
                children.append(FileTreeNode(
                    name=entry.name,
                    path=str(entry),
                    type="file",
                    size=stat.st_size,
                    extension=entry.suffix,
                    modified=datetime.fromtimestamp(stat.st_mtime).isoformat(),
                ))

        return children
```

---

## 六、文件读取设计

### 6.1 大文件分页读取

```python
class FileReader:
    """文件读取器"""

    def read(
        self,
        file_path: Path,
        start_line: int = 1,
        limit: int = 500,
    ) -> FileContent:
        """读取文件内容（分页）"""

        # 验证文件大小
        size = file_path.stat().st_size
        if size > MAX_FILE_SIZE:
            raise FileTooLargeError(
                f"文件过大: {size} bytes",
                "FILE_TOO_LARGE",
            )

        # 读取内容
        content = file_path.read_text(encoding="utf-8", errors="ignore")
        lines = content.splitlines()

        # 计算分页
        total_lines = len(lines)
        start_idx = max(0, start_line - 1)
        end_idx = min(total_lines, start_idx + limit)

        page_content = "\n".join(lines[start_idx:end_idx])

        return FileContent(
            path=str(file_path),
            name=file_path.name,
            size=size,
            total_lines=total_lines,
            content=page_content,
            truncated=len(content) > 50000,  # 超过50KB标记
            has_more=end_idx < total_lines,
        )
```

---

## 七、前端组件设计

### 7.1 文件浏览器布局

```
┌─────────────────────────────────────────────────────────────────┐
│ 面包屑: [项目] / [src] / [components]              [搜索框]   │
├──────────────────────┬──────────────────────────────────────────┤
│                      │                                          │
│   文件树             │   文件预览                                │
│   (250px 宽)        │   (flex: 1)                              │
│                      │                                          │
│  ▼ src/             │  1  │ import React from 'react';       │
│    ▼ components/   │  2  │                                   │
│      ├─ Header.js  │  3  │ export const Header = () => {     │
│      └─ Footer.js  │  4  │   return (                        │
│    └─ App.js       │  5  │     <header>...</header>         │
│  ▶ README.md        │  6  │   );                              │
│                      │  7  │ };                                │
│                      │                                          │
└──────────────────────┴──────────────────────────────────────────┘
```

### 7.2 前端模块结构

```
web/static/modules/fileExplorer/
├── index.js              # 主模块，初始化和事件绑定
├── api.js                # API 调用封装
├── state.js              # 状态管理
├── components/
│   ├── FileTree.js       # 文件树组件
│   ├── FilePreview.js    # 文件预览组件
│   ├── Breadcrumb.js     # 面包屑导航
│   └── SearchBox.js      # 搜索框
└── utils/
    ├── treeUtils.js      # 树操作工具
    └── fileUtils.js      # 文件操作工具
```

### 7.3 组件状态设计

```javascript
// 状态管理
const FileExplorerState = {
    // 当前路径
    currentPath: '/project',

    // 目录树数据
    tree: {
        loaded: false,
        data: null,
        expandedDirs: new Set(),
    },

    // 当前文件
    currentFile: {
        path: null,
        content: null,
        loading: false,
    },

    // 搜索
    search: {
        pattern: '',
        results: [],
        loading: false,
    },

    // UI 状态
    ui: {
        sidebarWidth: 250,
        showSearch: false,
    }
};
```

---

## 八、性能优化设计

### 8.1 后端优化

| 优化点 | 方案 |
|--------|------|
| 目录树缓存 | 同一路径的目录树缓存 30 秒 |
| 惰性加载 | 目录默认只加载一层，展开时动态加载 |
| 搜索限流 | 搜索请求间隔至少 300ms |
| 大文件流式读取 | 超过 1MB 使用流式读取 |

### 8.2 前端优化

| 优化点 | 方案 |
|--------|------|
| 虚拟滚动 | 文件列表使用虚拟滚动 |
| 懒加载 | 非可视区域不渲染 |
| 请求防抖 | 搜索输入防抖 300ms |
| 缓存 | 目录树缓存 localStorage |

---

## 九、测试策略

### 9.1 单元测试

| 模块 | 测试重点 |
|------|----------|
| PathValidator | 路径验证、敏感文件过滤 |
| TreeBuilder | 目录树构建、深度控制 |
| FileReader | 分页读取、编码处理 |
| API 端点 | 参数验证、响应格式 |

### 9.2 集成测试

| 场景 | 验证点 |
|------|--------|
| 正常读取 | 目录树返回正确结构 |
| 路径遍历攻击 | 返回 403 错误 |
| 敏感文件访问 | 返回 403 错误 |
| 大文件分页 | 分页参数正确 |
| 搜索功能 | Glob 模式匹配正确 |

---

## 十、部署配置

### 10.1 环境变量

| 变量 | 默认值 | 说明 |
|------|--------|------|
| WORKING_DIR | `.` | 允许访问的基础目录 |
| MAX_TREE_DEPTH | 5 | 目录树最大深度 |
| MAX_READ_LINES | 1000 | 单次读取最大行数 |
| MAX_FILE_SIZE | 10485760 | 文件大小限制 (10MB) |

---

## 十一、扩展性设计

### 11.1 未来扩展点

| 扩展点 | 说明 |
|--------|------|
| 云存储支持 | 抽象存储层，支持 S3 等 |
| 文件编辑 | 添加文件编辑功能 |
| 文件上传 | 添加文件上传功能 |
| 多选操作 | 支持批量操作文件 |

### 11.2 插件化设计

```python
class FileService(ABC):
    """文件服务基类"""

    @abstractmethod
    def get_tree(self, path: str, depth: int) -> FileTreeNode:
        pass

    @abstractmethod
    def read_file(self, path: str, start: int, limit: int) -> FileContent:
        pass

# 本地文件系统实现
class LocalFileService(FileService):
    pass

# 未来可扩展
class S3FileService(FileService):
    pass
```

---

**文档结束**

*本文档由高级架构师 (senior-architect) 生成，用于指导后续开发工作。*
